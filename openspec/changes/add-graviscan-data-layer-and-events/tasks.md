## 1. Shared test scaffolding

- [x] 1.1 Read `tests/integration/database.test.ts` and `tests/unit/graviscan/image-handlers.test.ts` fully to confirm this repo's convention for seeding Prisma test data (real SQLite test DB, not mocked Prisma client) and reuse it for every handler test below — no logic-mirror reimplementations (matches the standard already enforced during PR #270/#272's review).
- [x] 1.2 Add test fixtures/helpers for seeding two distinct `Experiment` rows, two `GraviScanner` rows, a `GraviScanSession`, and `GraviScan` rows across both experiments — needed by every cross-experiment-scoping test below.

## 2. `database.graviscans.*`

- [x] 2.1 Write failing test: `graviscans.create` rejects (or the underlying handler throws a clear error) when `experiment_id`/`phenotyper_id`/`scanner_id` are present but not strings (e.g. `123`, `{}`, arrays) — type-validation, not just truthiness, matching the critical fix already applied to `verify-plates` (a malformed IPC payload must not defeat scoping guarantees).
- [x] 2.2 Write failing test: `graviscans.create` persists all fields (including optional `session_id`/`cycle_number`/`wave_number`/`scan_started_at`/`scan_ended_at`) and defaults `format` to `'tiff'` and `wave_number` to `0` when omitted.
- [x] 2.3 Implement `graviscans.create` in `src/main/database-handlers.ts` to pass 2.1–2.2.
- [x] 2.3a Add an explicit code comment on `graviscans.create` (and mirror it in the spec — see `specs/scanning/spec.md`'s `graviscans.*` requirement): a future Tier 4/5 caller writing `GraviScan.resolution` from a completed scan MUST source it from that scan's `achieved_resolution` (Section 9's new field, threaded through the `scan-complete` event payload), not the pre-scan requested value it originally sent — otherwise the entire point of the #232 fix never reaches the queryable database record. This handler itself does not receive `achieved_resolution` (it's a pre-scan create, not a post-scan write path), so this is a forward-looking note for whichever change adds the completion-time write, not a change to this handler's own signature.
- [x] 2.4 Write failing test: `graviscans.getMaxWaveNumber` returns `-1` for an experiment with zero non-deleted `GraviScan` rows, and ignores rows where `deleted: true`.
- [x] 2.5 Write failing test: `graviscans.getMaxWaveNumber` returns the max `wave_number` scoped to the given `experimentId` only — seed a second experiment with a higher `wave_number` and assert it is NOT returned (cross-experiment scoping probe).
- [x] 2.6 Implement `graviscans.getMaxWaveNumber` to pass 2.4–2.5.
- [x] 2.7 Write failing test: `checkBarcodeUniqueInWave` returns `isDuplicate: true` when the exact barcode already exists in the same `(experiment_id, wave_number)` with `deleted: false`.
- [x] 2.8 Write failing test: `checkBarcodeUniqueInWave` treats `"ABC123"` and `"abc123"` as the same barcode within the same wave (case-insensitivity — see `design.md` Decision 4). Also assert leading/trailing whitespace variance (`" ABC123 "` vs `"ABC123"`) is treated as duplicate.
- [x] 2.9 Write failing test: `checkBarcodeUniqueInWave` does NOT flag a duplicate when the matching barcode exists in a *different* experiment or a *different* wave_number of the same experiment (cross-experiment / cross-wave scoping probe).
- [x] 2.10 Write failing test: `checkBarcodeUniqueInWave` ignores rows where `deleted: true`.
- [x] 2.11 Implement `checkBarcodeUniqueInWave` (case-insensitive comparison in application code, not a SQLite `mode: 'insensitive'` filter — unsupported on this datasource) to pass 2.7–2.10.
- [x] 2.12 Write failing test: `updateGridTimestamps` updates `scan_started_at`/`scan_ended_at` on every id in `ids` that belongs to the given `experiment_id`.
- [x] 2.13 Write failing test: `updateGridTimestamps` does NOT update a `GraviScan` row whose id is in `ids` but whose `experiment_id` does not match the passed `experiment_id` — this is the required scoping fix over the reference implementation (see `design.md` Decision 3); assert the foreign row's timestamps are unchanged after the call.
- [x] 2.14 Write failing test: `updateGridTimestamps` returns the count of rows actually updated (not `ids.length`) so a caller can detect a partial/cross-experiment mismatch.
- [x] 2.15 Implement `updateGridTimestamps` with the required `experiment_id` parameter and scoped `updateMany` to pass 2.12–2.14.
- [x] 2.16 Write failing test: `browseByExperiment` returns experiments paginated by `offset`/`limit`, each with nested non-deleted scans, and computes `hasNeedsReview` from `graviPlateAssignments` (main's relation field name — confirm via `prisma/schema.prisma:42` — differs from the reference implementation's `graviscanPlateAssignments`/`graviscanScans` relation names, which do not exist on `main`'s schema; use `graviScans`/`graviPlateAssignments` as declared).
- [x] 2.17 Write failing test: `browseByExperiment`'s date/experimentName/accession/uploadStatus filters each narrow results correctly (one test per filter), per the following semantics — determined by reading the reference implementation directly (`git show origin/fix/v600-wedge-followups-metadata_propogation_followup:src/main/database-handlers.ts`, the `db:graviscans:browse-by-experiment` handler) and restated here inline, translated to `main`'s field/relation names, so this scenario is self-contained and does not depend on an external, possibly-disappearing branch:
  - `dateFrom`/`dateTo` filter on each candidate experiment's non-deleted `GraviScan.capture_date` values and are **both inclusive**: `dateFrom` is applied as `capture_date >= dateFrom` (as given); `dateTo` is applied as `capture_date <= (dateTo, with time advanced to 23:59:59.999 of that calendar day)` — i.e. a `dateTo` of a given day includes scans captured any time during that day, not just up to midnight.
  - `experimentName` matches by **substring** (Prisma `contains`) against `Experiment.name` — not exact-match. Case sensitivity follows this datasource's default (SQLite, case-sensitive; `mode: 'insensitive'` is Postgres-only and not used here, consistent with Decision 4's SQLite constraint elsewhere in this proposal).
  - `accession` matches by **substring** against the linked `Accessions.name` (via `Experiment.accession`) — not by `Accessions.id` and not against any `GraviScan` field.
  - `uploadStatus` is **not** a Prisma-level filter — it is an in-memory post-filter evaluated per experiment over the aggregated `GraviImage.status` values across every one of that experiment's non-deleted scans: an experiment with zero images matches only `uploadStatus: 'pending'`; `'uploaded'` requires every image's `status` to be `'uploaded'`; `'failed'` requires at least one image's `status` to be `'failed'`; `'pending'` requires every image's `status` to be `'pending'`; any other `uploadStatus` value passes every experiment through unfiltered.
- [x] 2.18 Implement `browseByExperiment` to pass 2.16–2.17.
- [x] 2.19 Write failing test: `experimentDetail(experimentId)` returns `null`/`{success:false}` for a nonexistent experiment id, and for an existing one returns scans ordered by `(cycle_number, scanner_id, plate_index)` plus a `verificationStatusMap` keyed `"scannerId:plateIndex"`.
- [x] 2.20 Write failing test: `experimentDetail(experimentId)` never includes scans or plate assignments belonging to a different experiment, even when that experiment shares a scanner (cross-experiment scoping probe — the exact class of bug the `verify-plates` port fixed).
- [x] 2.21 Implement `experimentDetail` to pass 2.19–2.20.

## 3. `database.graviscanSessions.*`

- [x] 3.1 Write failing test: `graviscanSessions.create` persists `experiment_id`/`phenotyper_id`/`scan_mode` and defaults `interval_seconds`/`duration_seconds`/`total_cycles` to `null` when omitted.
- [x] 3.1a Write failing test: `graviscanSessions.create` rejects (or returns `{success:false}`) when `experiment_id`/`phenotyper_id` is a number or object instead of a string — type-validation probe, same class as 2.1 (`graviscans.*`) and 4.4 (`graviscanPlateAssignments.*`). Currently no analogous test exists for this handler group despite it accepting the same class of string-typed scoping identifiers — this closes that gap in the audit.
- [x] 3.2 Implement `graviscanSessions.create` to pass 3.1–3.1a.
- [x] 3.3 Write failing test: `graviscanSessions.complete` sets `completed_at` to a real timestamp and `cancelled` to the passed value (default `false`) for the given `session_id`.
- [x] 3.4 Write failing test: `graviscanSessions.complete` on a nonexistent `session_id` returns a clear `{success:false, error}` rather than throwing an unhandled Prisma error up through the IPC boundary.
- [x] 3.4a Write failing test: `graviscanSessions.complete` rejects (or returns `{success:false}`) when `session_id` is not a string — type-validation probe, same class as 2.1/4.4.
- [x] 3.5 Implement `graviscanSessions.complete` to pass 3.3–3.4a.

## 4. `database.graviscanPlateAssignments.*`

- [x] 4.1 Write failing test: `list(experimentId, scannerId)` returns only assignments matching both, ordered by `plate_index`, and returns none for a different `experimentId` sharing the same `scannerId` (cross-experiment scoping probe — a scanner can be shared/reused across experiments over time, so this is a realistic case, not a contrived one).
- [x] 4.2 Implement `list`.
- [x] 4.3 Write failing test: `upsertMany(experimentId, scannerId, assignments[])` creates new rows and updates existing ones (matched on the compound unique `experiment_id_scanner_id_plate_index`) within a single `db.$transaction`.
- [x] 4.4 Write failing test: `upsertMany` rejects (or the handler returns `{success:false}`) when `experimentId`/`scannerId` are not strings — type-validation probe, same class as 2.1.
- [x] 4.5 Write failing test: `upsertMany` is atomic — construct one assignment payload that violates a constraint (e.g. a `plate_index` value that collides with a manually-inserted row via a different unique path, or a deliberately-invalid `transplant_date` string) among otherwise-valid assignments, and assert NONE of the batch's rows are persisted after the call fails (transaction rollback probe).
- [x] 4.6 Implement `upsertMany` to pass 4.3–4.5.

## 5. `database.graviPlateAccessions.*`

- [x] 5.1 Write failing test: `createWithSections(accessionData, plates[])` creates one `Accessions` row, one `GraviPlateAccession` row per plate, and one `GraviPlateSectionMapping` row per section, all inside a single `db.$transaction`, and returns `totalPlates`/`totalSections` counts.
- [x] 5.2 Write failing test: `createWithSections` rolls back entirely (zero rows persisted, including the parent `Accessions` row) if any plate/section in the batch fails to insert (e.g. a duplicate `(gravi_plate_id, plant_qr)` pair) — transaction-atomicity probe, same class as 4.5.
- [x] 5.3 Implement `createWithSections` to pass 5.1–5.2.
- [x] 5.4 Write failing test: `list(metadataFileId)` returns plates naturally sorted by `plate_id` (e.g. `"P2"` before `"P10"`, not lexicographic) with sections naturally sorted by `plate_section_id`, and returns an empty list (not an error) for a `metadataFileId` with zero plates.
- [x] 5.4a Write failing test: `list` rejects (or returns `{success:false}`) when `metadataFileId` is not a string — type-validation probe, same class as 2.1/4.4. `graviPlateAccessions.*` currently has no analogous test despite accepting the same class of string-typed scoping identifier.
- [x] 5.5 Implement `list` (port or reimplement the reference's natural-sort comparator; verify it actually produces natural order via 5.4, don't assume `naturalCompare`'s reference behavior) to pass 5.4–5.4a.
- [x] 5.6 Write failing test: `listFiles()` returns only `Accessions` rows that have at least one `GraviPlateAccession` child, each annotated with its linked experiment names and plate count. Confirm and document in the test/comment that this handler takes no filesystem path argument (see `design.md` Open Question 5) — do not add a path-containment check that has no corresponding input to validate.
- [x] 5.7 Implement `listFiles`.
- [x] 5.8 Write failing test: `delete(metadataFileId)` is blocked (returns `{success:false, error}`, deletes nothing) when the target is linked to any `Experiment.accession_id` — reuse the reference implementation's `countMetadataReferences` concept but scoped to what exists on `main` (no `graviExperimentWaveMetadata` count term — see `design.md` Decision 1; only the `Experiment.accession_id` count applies).
- [x] 5.8a Write failing test: `delete` rejects (or returns `{success:false}`) when `metadataFileId` is not a string — type-validation probe, same class as 2.1/4.4.
- [x] 5.9 Write failing test: `delete(metadataFileId)` on an unlinked accession removes the `Accessions` row and cascades its `GraviPlateAccession`/`GraviPlateSectionMapping` children inside one transaction; assert zero orphaned section rows remain.
- [x] 5.10 Implement `delete` to pass 5.8–5.9, 5.8a.

## 6. Metadata-linking descope (documentation only, no implementation)

- [x] 6.1 Confirm (re-run the check, don't trust the design doc's snapshot) that `prisma/schema.prisma` still has no `GraviExperimentWaveMetadata` model as of implementation time.
- [x] 6.2 Do NOT implement `experiments.listGraviMetadata`/`linkGraviMetadata`/`unlinkGraviMetadata` in this change (see `design.md` Decision 1). Leave a code comment at the relevant spot in `src/main/database-handlers.ts` (near the other `experiments.*` handlers) noting the descope and pointing at this change's `design.md` Open Question 1, so a future implementer doesn't assume the omission was accidental.

## 7. Issue #234 — verify already fixed, no code change

- [ ] 7.1 Re-read `src/main/graviscan/register-handlers.ts`'s `graviscan:save-scanners-db` handler at implementation time (code may have moved since this proposal was written) and confirm the `#234` fix (spawn-worker-for-newly-saved-enabled-scanner loop) is still present and unchanged.
- [ ] 7.2 Add (if not already present) a unit test in `tests/unit/graviscan/register-handlers.test.ts` asserting: given a `save-scanners-db` payload containing one brand-new scanner with `usb_bus`/`usb_device` set, the coordinator's `addScanner()` is called exactly once for that scanner's id after the DB upsert succeeds (regression-lock the existing fix rather than merely reading the code).
- [ ] 7.3 No handler code changes. Note in the PR description that #234 was investigated and found already fixed, with the exact file/line evidence, so it can be closed on GitHub.

## 8. Issue #231 — not applicable to `main`'s architecture

- [ ] 8.1 Re-confirm at implementation time that `src/main/graviscan/scanner-upsert.ts`'s `UpsertScannerPayload` and both the UPDATE/CREATE Prisma calls still have no `grid_mode` field, and that `prisma/schema.prisma`'s `GraviScanner` model still has no `grid_mode` column.
- [ ] 8.2 No handler code changes. Note in the PR description that #231's reported bug class (per-scanner `grid_mode` silently dropped on UPDATE) cannot reproduce on `main` because there is no per-scanner `grid_mode` field for an UPDATE to omit — `main` deliberately sources `grid_mode` from the `GraviConfig` singleton (already documented in `scanner-handlers.ts`'s `getScannerStatus()` docstring) — so it can be annotated as not-applicable on GitHub.

## 9. Issue #232 — backend correctness half (achieved-resolution readback)

- [ ] 9.1 Write failing Python test: `_sane_scan()` (or an extracted helper) reads back `self._device.x_resolution`/`y_resolution` after setting them, and includes the achieved value in the returned/logged data — mock the SANE device object to report a different value than requested and assert the mismatch is captured, not silently discarded.
- [ ] 9.1a Write failing Python test (the positive/negative-space case for 9.1 — currently completely untested, so a comparison bug like `!=` accidentally written as `==` would not be caught): when the SANE device accepts the requested resolution exactly (mocked to report back the same value), the readback SHALL report no mismatch, and no warning SHALL be logged.
- [ ] 9.2 Write failing Python test: a resolution mismatch (device reports a different value than requested) logs a warning via the existing `log()` helper, including both the requested and achieved values in the message.
- [ ] 9.3 Write failing Python test: the TIFF metadata embedding (`_build_tiff_metadata` or wherever `resolution` is embedded) and the emitted `scan-complete` event payload both include an `achieved_resolution` field reflecting the device's actual value, not the caller's requested value.
- [ ] 9.4 Implement the readback + logging + payload field in `python/graviscan/scan_worker.py` to pass 9.1, 9.1a, 9.2, 9.3.
- [ ] 9.5 Write failing TS test: `ScannerSubprocess`'s typed `scan-complete` event (and the coordinator's forwarded `scan-complete`, once Section 10 lands) passes `achieved_resolution` through unchanged when the worker's event payload includes it.
- [ ] 9.6 Update `src/main/graviscan/scanner-subprocess.ts`'s event typing/forwarding (additive field only, no behavior change to existing fields) to pass 9.5.
- [ ] 9.7 Run the full `python/tests/test_scan_worker.py` suite and confirm no existing test needed a behavior change (only additive fields) — flag in the PR if any existing test needed adjustment, since that would indicate an unintended behavior change.

> **⚠️ Commit/merge discipline for Sections 10–11, read before starting either section.** After task 10.7 lands (the coordinator stops emitting the generic `scan-event` bus, per task 10.6's zero-invocation assertion), `wiring.ts`'s `setupWedgeDetection()` still only listens for the now-dead `scan-event` channel until task 11.5 rewrites it to consume the three new granular channels. **Every commit in that window has ZERO wedge events reaching `WedgeDetector` — not degraded, completely blind, on a safety-relevant path.** Tasks 10.7 through 11.5 (inclusive) MUST be committed together as a single commit, or this PR must be squash-merged before landing on `main`. **Do not bisect into or checkout an intermediate commit in this range for any reason** — there is no functional wedge detection anywhere in it. (Also documented in `design.md`'s Risks / Trade-offs section.)

## 10. Event model migration: `ScanCoordinator`

- [ ] 10.1 Write failing test: given a mocked `ScannerSubprocess` that emits a generic `event` with `type: 'scan-started'`, `scanner_id`, `plate_index`, the coordinator emits `scan-started` (not `scan-event`) with `jobId` set to `` `${scannerId}:${plateIndex}` ``, plus `scannerId`/`plateIndex`/`cycle_number`/`scan_started_at`.
- [ ] 10.2 Write failing test: same setup with `type: 'scan-complete'` → coordinator emits `scan-complete` with `jobId`/`scannerId`/`plateIndex`/`path` (and `achieved_resolution` if present on the source event, from Section 9).
- [ ] 10.2a Retarget the existing test `'forwarded scan-event does not include scan_ended_at before row completes'` in `tests/unit/graviscan/scan-coordinator.test.ts` (currently ~line 371, does `coordinator.on('scan-event', scanEvent)`) to listen on `coordinator.on('scan-complete', ...)` instead. The invariant it checks (a `scan-complete` emitted before the row finishes omits `scan_ended_at` but includes `scan_started_at`) is still required by the new spec (see the "Forwarded scan events use granular per-job channels" scenario in `specs/scanning/spec.md`) — do not delete this test, retarget it. This is one of four confirmed-breaking test files identified during review; see the note at task 10.6 and Section 11/12 for the other three.
- [ ] 10.3 Write failing test: same setup with `type: 'scan-error'` (subprocess-originated) → coordinator emits `scan-error` with `jobId`/`scannerId`/`plateIndex`/`error`/`bytes_received`/`wall_seconds`.
- [ ] 10.4 Write failing test: the coordinator's own row-timeout emission (`scanOnce()`'s `SCAN_ROW_TIMEOUT_MS` branch) emits on the SAME `scan-error` channel as 10.3, with `jobId` derived from `scannerId` alone when no single `plateIndex` applies to the whole row (document the exact `jobId` shape chosen — e.g. bare `scannerId` — in a code comment, since this is the one case where `jobId` isn't `scannerId:plateIndex`).
- [ ] 10.5 Write failing test: the coordinator's own missing-output-file and zero-size-output-file emissions (`scanOnce()`'s file-verification block) emit on the same unified `scan-error` channel with a real `jobId` (`scannerId:plateIndex`, since these ARE per-plate).
- [ ] 10.5a Write failing test covering the **4th** direct `scan-error` emit site, distinct from 10.4/10.5: the coordinator's "cannot stat output file" branch (`scanOnce()`'s file-verification block, the `fs.promises.stat()` call's `catch` — separate from the `fs.promises.access()` missing-file check 10.5 already covers) emits on the same unified `scan-error` channel. Confirmed by reading `src/main/graviscan/scan-coordinator.ts` lines 522–601 directly: there are 4 total direct `this.emit('scan-error', ...)` call sites in this file (row-timeout, missing-output-file, cannot-stat-file, zero-size-file), not 3 as earlier drafts of this proposal claimed. This site is structurally identical to 10.5's two sites (same per-plate loop, `plateIndex` in scope), so it SHALL use the same per-plate `` `${scannerId}:${plateIndex}` `` `jobId` shape as 10.5 — NOT the bare-`scannerId` shape used by 10.4's row-timeout site.
- [ ] 10.6 Write failing test: `scan-event` is NEVER emitted by the coordinator anymore (assert zero listener invocations for that event name across a full `scanOnce()` run that includes a success, a subprocess error, and a verification failure). **Note**: this test passing does NOT mean wedge detection is safe at this point in the commit sequence — see the commit-discipline warning above Section 10. Landing this task alone, without also landing Section 11's `wiring.ts` rewrite in the same commit, leaves `setupWedgeDetection()` listening for an event that will never fire again.
- [ ] 10.7 Implement the coordinator changes in `src/main/graviscan/scan-coordinator.ts` to pass 10.1–10.6, 10.2a, 10.5a: replace the `sub.on('event', ...)` handler's single `this.emit('scan-event', forwarded)` call with type-routed emission to the three new channels, and update the 4 direct `scan-error` emit call sites in `scanOnce()` (row-timeout, missing-output-file, cannot-stat-file, zero-size-file) to include `jobId`.
- [ ] 10.8 Confirm `grid-start`/`grid-complete`/`cycle-complete`/`interval-*`/`overtime`/`cancelled`/`scanner-init-status` emissions are byte-for-byte unchanged (run the existing, non-`scan-event`-related tests in `tests/unit/graviscan/scan-coordinator.test.ts` and confirm they pass with zero modifications needed).

## 11. `wiring.ts`: update `setupWedgeDetection()` and event forwarding

- [ ] 11.1 Write failing test (extend `tests/unit/graviscan/main-wiring.test.ts`'s `coordinator event forwarding` block): `setupCoordinatorEventForwarding()` forwards `scan-started`/`scan-complete`/`scan-error` (replacing `scan-event`) plus the **9** unchanged events (`grid-start`, `grid-complete`, `cycle-complete`, `interval-start`, `interval-waiting`, `interval-complete`, `overtime`, `cancelled`, `scanner-init-status` — per `design.md` Decision 2's list; the previous draft of this task said "8", which would have silently dropped `scanner-init-status` from the updated event-forwarding test array if implemented literally) — assert the exact new list, update the `'forwards all 11 coordinator events'` test's event-name array, name, and count (11 → 12: drop `scan-event`, add `scan-started` and `scan-complete`, keep `scan-error` and the 9 unchanged events).
- [ ] 11.1a Retarget the two `mainWindow`-null/destroyed guard tests in the same `coordinator event forwarding` block (`'does not crash when mainWindow is null'`, `'does not crash when mainWindow is destroyed'`, currently ~lines 228–253) to `coordinator.emit('scan-error', ...)` instead of `coordinator.emit('scan-event', ...)`. Once `scan-event` is removed, these tests would still "pass" but only because the emitted event matches no listener — silently degrading to no-ops instead of actually exercising the guard. Use `scan-error` (or any surviving event name) so the assertions keep testing real guard behavior.
- [ ] 11.2 Actual case count for `setupWedgeDetection`'s describe block in `tests/unit/graviscan/main-wiring.test.ts`, counted directly (not the "~15" an earlier draft of this proposal estimated, which conflated cases that actually live in `tests/unit/wedge-detector.test.ts`/`tests/unit/wedge-pipeline-integration.test.ts` — those two files test `WedgeDetector`/`SlackNotifier` in isolation via their own inline harness, never reference `scan-event` or any coordinator channel name, and need NO changes for this task): **11 cases**. Retarget every one of them from `coordinator.emit('scan-event', {type: ..., ...})` to the new first-class channel names (`scan-error`, `scan-complete`) with no embedded `type` field. Split into the following independently-checkable sub-tasks — do not delete coverage, every existing assertion must have a surviving equivalent test:
  - [ ] 11.2a **Slack-fire end-to-end + session-id resolution** (2 cases): `'interval-start → scan-error (sane_start_invalid) → Slack POST + scanLog fire end-to-end, using the active session id'`; `'falls back to a startedAt-based session id when no active scan session exists'`.
  - [ ] 11.2b **Gating conditions — when a `scan-error` must NOT fire a Slack POST** (2 cases): `'does not POST to Slack when scan-event fires before any interval-start (no detector yet)'` (rename to reference `scan-error`, not `scan-event`, once retargeted); `'does not POST to Slack when the webhook env var is unset (feature disabled)'`.
  - [ ] 11.2c **Consecutive-failures dedup, recovered-path, and interval-complete teardown** (3 cases): `'routes two same-cycle scan-error events for one scanner to a single consecutive_failures wedge'`; `'scan-complete resolves a scanner without triggering a wedge (recovered path)'`; `'tears down the detector on interval-complete — a later scan-error no longer triggers a wedge'`.
  - [ ] 11.2d **Slack-message enrichment — success/no-db/db-null/db-error** (4 cases): `'enriches the Slack message with display_name/usb_port looked up from the db (final-review #4)'`; `'falls back to the unenriched event when no db is passed'`; `'falls back to the unenriched event when the scanner_id is not found in the db'`; `'falls back to the unenriched event when the db lookup throws'`.
- [ ] 11.3 Write failing test: `setupWedgeDetection()` calls `wedgeDetector.onScanEnd({success:true})` when a `scan-complete` event arrives (replacing the old `event.type === 'scan-complete'` branch).
- [ ] 11.4 Write failing test (the found-bug fix from `design.md` Decision 2): a coordinator-originated `scan-error` (simulating the row-timeout/file-verification path — no `type` field needed since it's now a first-class channel) reaches `wedgeDetector.onScanError(...)` and can contribute to a `consecutive_failures` wedge, proving the previously-invisible-to-wedge-detection failure class is now visible.
- [ ] 11.4a Before requesting the explicit sign-off Open Question 2 asks for (coordinator-originated `scan-error` now counting toward `consecutive_failures`), backtest the new wedge-sensitivity logic against the production rig's actual historical scan-error/row-timeout log volume (`scanLog()` output / saved session logs) to estimate how many additional wedge alerts this change would have produced historically. Report the estimate alongside the sign-off request — see `design.md` Risks/Trade-offs for why this is cheap and worth doing before shipping a safety-relevant sensitivity change.
- [ ] 11.5 Implement the `setupWedgeDetection()` rewrite in `src/main/graviscan/wiring.ts` to pass 11.1–11.4 (and 11.1a, 11.2a–11.2d): replace the single `coordinator.on('scan-event', ...)` listener with three listeners (`scan-started` → `onCycleStart` when cycle_number changes, `scan-error` → `onScanError` + `onScanEnd({success:false})`, `scan-complete` → `onScanEnd({success:true})`). **Per the commit-discipline warning above Section 10, this task and task 10.7 MUST land in the same commit.**
- [ ] 11.6 Run the complete `tests/unit/graviscan/main-wiring.test.ts` file and confirm 100% of its pre-existing test count still exists (renamed/retargeted, not deleted) and passes.
- [ ] 11.7 Retarget the third affected test in `tests/unit/graviscan/main-wiring.test.ts`'s `coordinator lazy instantiation` describe block — `'threads the db passed to initGraviScan through to setupWedgeDetection for wedge-event enrichment (final-review #4)'` (currently ~line 538) — which does `coordinator.emit('scan-event', {type: 'scan-error', ...})`. This test is NOT covered by 11.1's scope (event-forwarding count) or 11.2's scope (the `setupWedgeDetection` describe block) since it lives in a third, separate describe block; it needs its own explicit retargeting to `coordinator.emit('scan-error', {...})` with no embedded `type` field. This was one of four confirmed-breaking test files with no retargeting task assigned in earlier drafts of this proposal — the other three are covered by 10.2a, 12.1/12.1a (below), and 12.6.

## 12. `preload.ts` + `electron.d.ts`

- [ ] 12.1 **Correct file pointer** (an earlier draft of this task pointed at `tests/unit/graviscan-types.test.ts`, which only tests unrelated constants and has no `onScanEvent`/`graviAPI`-shape assertions at all): the actual test file that breaks is `tests/unit/preload-gravi.test.ts`, which has 5 tests tied to `onScanEvent` that WILL fail once it's removed. Retarget all 5, don't delete them:
  - [ ] 12.1a `'has all 12 event listener methods'` (the `listenerMethods` array, ~line 98): update to 13 methods — remove `onScanEvent`, add `onScanStarted` and `onScanComplete` (12 − 1 + 2 = 13); rename the test to `'has all 13 event listener methods'`.
  - [ ] 12.1b `'onScanEvent registers listener on correct channel'` (~line 119): retarget to `onScanStarted` (or `onScanComplete`), asserting registration against `'graviscan:scan-started'` (or `'graviscan:scan-complete'`).
  - [ ] 12.1c `'onScanEvent returns cleanup function'` (~line 128): retarget the same way.
  - [ ] 12.1d `'cleanup function calls removeListener'` (~line 134): retarget the same way, asserting `removeListener` is called with the new channel name.
  - [ ] 12.1e `'listener invokes callback with event data'` (~line 144): retarget the same way, asserting the callback receives the simulated payload on the new channel.
- [ ] 12.2 Implement the `preload.ts` changes: remove `onScanEvent`, add `onScanStarted`/`onScanComplete` (registering `ipcRenderer.on('graviscan:scan-started'/'graviscan:scan-complete', ...)` with the same cleanup-function pattern every other listener already uses) to pass 12.1a–12.1e.
- [ ] 12.3 Write failing test: `window.electron.database.graviscans`, `.graviscanSessions`, `.graviscanPlateAssignments`, `.graviPlateAccessions` each expose exactly the methods implemented in Sections 2–5 (no extra, no missing) — a contract test, not a re-implementation.
- [ ] 12.4 Implement the `preload.ts` `databaseAPI` additions for the four new namespaces, and the corresponding `DatabaseAPI` interface additions in `src/types/electron.d.ts`.
- [ ] 12.5 Run `npx tsc --noEmit` and confirm zero new type errors introduced by this change (the one pre-existing, unrelated `graviscan-upload.ts:278` error is expected and untouched).
- [ ] 12.6 Retarget `tests/e2e/graviscan-ipc.e2e.ts`'s `'event listener returns cleanup function'` test (~line 224), which currently calls `electron.gravi.onScanEvent(() => {})` directly. This is an E2E test that runs in CI and WILL fail once `onScanEvent` is removed. Retarget it to call `electron.gravi.onScanStarted(() => {})` (or `onScanComplete`/`onScanError`) instead, keeping the same cleanup-function assertion. This was one of four confirmed-breaking test files with no retargeting task assigned in earlier drafts of this proposal (the other three are 10.2a, 11.7, and 12.1a–12.1e above). Add this file to task 13.3's verification run command list.

## 13. Spec, validation, full suite

- [ ] 13.1 Write the spec delta at `openspec/changes/add-graviscan-data-layer-and-events/specs/scanning/spec.md` (ADDED requirements for the 4 new `database.*` handler groups; MODIFIED for `ScanCoordinator Multi-Scanner Orchestration`'s forwarded-events scenario, `GraviScan Coordinator Event Forwarding`, and `GraviScan Preload Context Bridge`).
- [ ] 13.2 Run `npx openspec validate add-graviscan-data-layer-and-events --strict` and fix all reported issues.
- [ ] 13.3 Run the full suite: `npm run test:unit`, `uv run pytest python/tests/test_scan_worker.py`, `npx tsc --noEmit`, `npm run lint`, `npm run format:check`, `npx playwright test tests/e2e/graviscan-ipc.e2e.ts` (added explicitly — task 12.6 retargets this file's one broken test, and it must be run, not just assumed fixed by inspection) — all green, with only the documented pre-existing/unrelated failures (Windows path-separator assertions, the `graviscan-upload.ts:278` type error, etc.). Use the following concrete method to re-verify these are still the same known set, not new failures wearing the same description — "re-verify" is not itself a method:
  1. **Before starting implementation**, run the full suite on `main` and capture the exact list of failing test names/IDs (not prose descriptions) into a baseline snapshot file (e.g. saved to a scratch file, not committed).
  2. **After implementation is complete**, run the full suite again and capture the new failure list the same way.
  3. **Diff the two lists by test ID/name**, not by reading and comparing prose descriptions of what each failure "is about" — a test ID that's new, or a test ID whose failure message changed in a way that suggests a different root cause, must be individually investigated and justified before being lumped in with "the known pre-existing set." Do not assume a failure matches the baseline just because it superficially resembles one of the documented pre-existing issues.
- [ ] 13.4 Update this file's checkboxes to reflect actual completion state before requesting review — do not mark a task `[x]` unless its test was actually run and observed passing.
