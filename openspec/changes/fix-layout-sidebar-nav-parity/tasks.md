## 1. Shell Background and Sidebar Nav-Link Recolor (#328 piece 1)

- [x] 1.1 Extend `tests/unit/pages/Layout.test.tsx`'s `renderLayout()` helper (`:82-105`) with a `path="*"` catch-all child route (e.g. `<Route path="*" element={<div>Other content</div>} />`, matching `App.tsx:128`'s existing production pattern) so `initialPath` can target any sidebar route — today it only registers `/`, `/configure-scanner`, `/metadata`, `/metadata-importing`, and React Router v6 renders nothing for the whole `<Routes>` tree when no route matches, which would otherwise make `Layout` fail to mount for any other `initialPath` and hang every `waitFor` in a new test.
- [x] 1.2 Write failing tests in `tests/unit/pages/Layout.test.tsx`:
  - The outer shell `<div>` (`Layout.tsx:432`) has `bg-stone-100`, not `bg-gray-50` — assert in both `cylinderscan` and `graviscan` mode.
  - Add a `describe('Layout nav-link colors')` block. Render at `initialPath: '/'` (Home — already registered, and Home is present in every mode, so this doesn't depend on the new catch-all route). Assert the active Home link's className contains `bg-stone-200`, `text-lime-800`, and `border-r-4 border-lime-800`. Separately assert a **non-active** link's className contains `hover:bg-stone-100` and `hover:text-lime-800` (hover classes are always present in the className string regardless of `:hover` pseudo-state — jsdom doesn't need to simulate an actual mouse hover to see them). Assert no link anywhere has any `blue-*` class.
  - Confirm all new assertions fail against current code (`Layout.tsx:432` bare `bg-gray-50`; `:448` `hover:bg-blue-50 hover:text-blue-600`; `:450` `bg-blue-50 text-blue-600 border-r-4 border-blue-600`). **Confirmed: all 5 new tests failed as expected; 20 pre-existing tests in this file still passed.**
- [x] 1.3 Implement: `Layout.tsx:432` `bg-gray-50` → `bg-stone-100`. `Layout.tsx:448` `hover:bg-blue-50 hover:text-blue-600` → `hover:bg-stone-100 hover:text-lime-800`; `:450` `bg-blue-50 text-blue-600 border-r-4 border-blue-600` → `bg-stone-200 text-lime-800 border-r-4 border-lime-800`. Confirm 1.2 passes. (`lime-800`, not `lime-700` — see `design.md`'s "nav-link color" decision: `lime-700` on `bg-stone-200` computes to ≈3.98:1 contrast, below WCAG AA's 4.5:1 minimum for normal text; `lime-800` clears it at ≈5.64:1/≈6.49:1.) **All 25 tests in Layout.test.tsx pass.**
- [x] 1.4 Run `npm run lint && npx tsc --noEmit && npm run test:unit`; fix any fallout. **Baseline established** (fresh worktree — `npm install` + `npm run prisma:generate` were needed first, unrelated to this change): lint clean, `tsc --noEmit` clean. Full unit suite: 1944 passed, 115 skipped, 2 pre-existing failing files unrelated to this change — `tests/unit/scans-export.test.ts` (missing local `prisma/dev.db` in this fresh worktree) and `tests/unit/graviscan/database-handlers.test.ts` (missing `BLOOM_DATABASE_URL` env var) — both environment-setup gaps, not code defects; not touched, per the established convention of not improvising database/env fixes (see project memory on the prior ad-hoc-script `.env` incident). **Commit checkpoint.**

## 2. Sidebar Link Restructuring and Ordering (#337, both modes)

- [x] 2.1 Write failing tests in `tests/unit/pages/Layout.test.tsx`'s existing `describe('Layout nav links')` block: in `cylinderscan` mode, all nine links appear in DOM order Home, Camera Settings, Capture Scan, Browse Scans, Export Scans, Scientists, Phenotypers, Accessions, Experiments (use `screen.getAllByRole('link')` and assert the `name`/`href` sequence, not just presence — presence is already covered by existing tests). In `graviscan` mode, all eight links appear in order Home, Configure Scanner, Capture Scan, Browse GraviScans, Scientists, Phenotypers, Metadata, Experiments. Add a regression-guard test that the default/no-mode order (`mode={null}`) is unchanged: Home, Scientists, Phenotypers, Experiments, Browse Scans, Export Scans. Confirm all three fail against current code (verified current orders are in `design.md`). **Confirmed: the two mode-specific ordering tests failed exactly as predicted (both had Setup links landing after Experiments in the received order); the default/no-mode test passed immediately since that order was never changed.**
- [x] 2.2 Implement in `Layout.tsx`: replace `alwaysLinks` (`:56-137`) with a standalone `homeLink` (Home only) and `setupLinks` (Scientists, Phenotypers **only** — do NOT include Experiments in this shared array; see the critical note below). Move the `Accessions` link object literal (`:238-257`) out of `captureLinks` into a standalone `accessionsLink`. Move the `Metadata` link object literal (`:302-321`) out of `graviscanLinks` into a standalone `metadataLink`. Extract `Experiments` (currently part of `alwaysLinks`) into its own standalone `experimentsLink`. Update the `links` composition (`:385-389`) to:
  - cylinderscan: `[homeLink, cameraSettingsLink, captureScanLink, browseScansLink, exportScansLink, ...setupLinks, accessionsLink, experimentsLink]`
  - graviscan: `[homeLink, configureScannerLink, captureScanLink, browseGraviscansLink, ...setupLinks, metadataLink, experimentsLink]`
  - default/no-mode: `[homeLink, ...setupLinks, experimentsLink, browseScansLink, exportScansLink]`

  **Critical — verified by two independent round-1 reviewers:** `experimentsLink` MUST be appended *after* `accessionsLink`/`metadataLink` in the cylinderscan/graviscan compositions (not folded into `setupLinks` and left to spread first) — spreading a `setupLinks` array that already ends in `Experiments` and then appending one more link after it can only ever place that link *last*, producing `...Experiments, Accessions` instead of the required `...Accessions, Experiments`. `setupLinks` must contain only Scientists and Phenotypers; `experimentsLink` is composed separately, last, in every mode. Keep each link's route/label/icon identical to today's — this task reorders and regroups object literals, it does not change any link's content. Confirm 2.1 passes. **All 28 tests in Layout.test.tsx pass.**
- [x] 2.3 Run `npm run lint && npx tsc --noEmit && npm run test:unit`; fix any fallout. Lint/tsc clean. Full suite: 1947 passed, 115 skipped, same 2 pre-existing baseline failures as task 1.4 (unrelated environment gaps). **Commit checkpoint.**

## 3. GraviScanWorkflowGuide Component (#328 piece 2)

- [x] 3.1 Write a new `tests/unit/components/GraviScanWorkflowGuide.test.tsx`, mirroring `tests/unit/components/CylinderScanWorkflowGuide.test.tsx`'s structure exactly (same `vi.mock('react-router-dom', ...)` pattern, same `mockNavigate` assertions). Cover:
  - A "Daily Workflow" section renders before a "Setup" section (`compareDocumentPosition`).
  - Daily Workflow contains, in order, Configure Scanner, Capture Scan (marked/rendered as the primary large CTA), Browse GraviScans — with no step-number badge.
  - Setup contains Scientists, Phenotypers, Metadata, Experiments as unordered cards with no step-number badge.
  - `data-testid`s use the same `workflow-step-${id}` slug scheme as `CylinderScanWorkflowGuide` (e.g. `workflow-step-configure-scanner`, `workflow-step-capture-scan`), and clicking one navigates to its route: `/configure-scanner`, `/capture-scan`, `/browse-graviscans`, `/scientists`, `/phenotypers`, `/metadata`, `/experiments` (all unchanged from today's `graviScanSteps` routes, except Configure Scanner, which is newly represented here for the first time).
  - Configure Scanner's card description text reads "Check scanner detection and connection health — especially after moving cables or a prior scan failure" (condition-specific framing, not a blanket "verify every session" claim — see `design.md`'s "Copy framing" note) — assert this exact copy so a future edit can't silently drift from it.
  - **Styling assertions, matching the exact pattern `CylinderScanWorkflowGuide.test.tsx:111-123` already uses** (do not assert a single combined class string on the testid'd button element): the Daily Workflow primary card's *button* (`workflow-step-capture-scan`) has `bg-lime-700` in its className; its *nested heading* (queried via `within(...).getByRole('heading')` or similar, not the button itself) has `text-white` — `text-white` lives on the card's inner `<h3>`, not the outer button, in the existing `CylinderScanWorkflowGuide.tsx` pattern this component copies. Non-primary cards' buttons have `hover:bg-lime-50` in their className.
  - Confirm it fails (component doesn't exist yet). **Confirmed: module-not-found failure, as expected.**
- [x] 3.2 Implement `src/renderer/components/GraviScanWorkflowGuide.tsx`, copying `CylinderScanWorkflowGuide.tsx`'s structure (its `DailyWorkflowStep` interface shape, card markup — including the `text-white` class living on the nested `<h3>`, not the button — and lime styling) with GraviScan's own step data: Daily Workflow = Configure Scanner (`/configure-scanner`, "Check scanner detection and connection health — especially after moving cables or a prior scan failure"), Capture Scan (`/capture-scan`, primary), Browse GraviScans (`/browse-graviscans`); Setup = Scientists (`/scientists`), Phenotypers (`/phenotypers`), Metadata (`/metadata`), Experiments (`/experiments`). Confirm 3.1 passes. **All 9 tests pass.**
- [x] 3.3 Run `npm run lint && npx tsc --noEmit && npm run test:unit`; fix any fallout. Lint/tsc clean. Full suite: 1956 passed, 115 skipped, same 2 pre-existing baseline failures. **Commit checkpoint.**

## 4. Wire GraviScanWorkflowGuide into Home; Retire graviScanSteps

- [x] 4.1 Update `tests/unit/pages/Home.test.tsx`:
  - Replace the `'renders GraviScan workflow steps when mode is graviscan'` test (`:87-101`) with an assertion that `GraviScanWorkflowGuide`'s "Daily Workflow"/"Setup" sections render (mirroring the cylinderscan-mode test at `:66-85`) instead of the generic "Workflow Steps" heading.
  - Replace the `'GraviScan step 5 (Capture Scan) navigates to /capture-scan'` test (`:120-133`), which clicks the old numeric `workflow-step-5` testid, with a click on `workflow-step-capture-scan` (mirroring how Tier 4 retired CylinderScan's numeric testids in favor of `workflow-step-scientists`, etc.).
  - **Also update** the `"does not fetch getRecent or render Today's Activity in graviscan mode"` test (`:211-224`), which waits on `screen.getByText('Workflow Steps')` (`:219`) as its readiness signal — the same now-dead heading text the two tests above replace. Change its readiness wait to `screen.getByText('Daily Workflow')` instead. (Found by round-1 TDD review: this third consumer of the retired heading isn't in the two tests tasks.md originally called out, and would otherwise hang/fail post-4.2 with no task flagging it.)
  - Keep every other existing assertion in this file (Today's Activity, config-exists redirect, cylinderscan-mode tests) untouched. Confirm the new/changed assertions fail against current code first. **Confirmed: all 3 changed tests failed as expected (2 timed out waiting for retired text, 1 for the retired testid); 13 other tests in the file still passed.**
- [x] 4.2 Update `Home.tsx`: replace the `mode === 'graviscan'` branch (`:127-136`, currently `<><h2>Workflow Steps</h2><WorkflowSteps steps={graviScanSteps} /></>`) with `<GraviScanWorkflowGuide />`, removing the now-redundant outer "Workflow Steps" heading (GraviScanWorkflowGuide has its own "Daily Workflow"/"Setup" sub-headers, same reasoning Tier 4 applied to the CylinderScan branch). Remove the now-unused `WorkflowSteps`/`graviScanSteps` import (`:4`); add the `GraviScanWorkflowGuide` import. Confirm 4.1 passes. **All 16 tests in Home.test.tsx pass.**
- [x] 4.3 Update the two other consumers of the retired numeric `workflow-step-N` testid scheme, found during round-1 review by grepping for `workflow-step-` (not just the `WorkflowSteps`/`graviScanSteps`/`WorkflowStep` identifiers — see 4.4's note on why that narrower grep alone is insufficient):
  - `tests/unit/pages/App.test.tsx:262-284`'s test asserting `expect(screen.queryByTestId('workflow-step-1')).not.toBeInTheDocument()` (lines 277, 282) as a regression guard against an accidental redirect-to-Home. Once numeric testids retire, this assertion becomes vacuously true regardless of whether the redirect bug is present, silently defeating its own purpose. Replace it with an equivalent guard against the new slug scheme (e.g. assert a GraviScan-specific route's own content is showing, or that no `workflow-step-` prefixed testid from the Home guide is present) so the regression guard still means something. **Implemented: replaced with `screen.queryByText('Daily Workflow')` absence — a redirect-to-Home in graviscan mode would render GraviScanWorkflowGuide's "Daily Workflow" heading, so its absence is still a real regression guard. All 11 tests in App.test.tsx pass.**
  - `tests/e2e/graviscan-browse-metadata.e2e.ts:300-321`'s test, which clicks `[data-testid="workflow-step-3"]` (line 319) to navigate to Metadata — a hard-coded numeric testid from `graviScanSteps`' old step 3. Update the selector to `workflow-step-metadata`, the new slug. **Implemented** (not run locally — E2E requires a manually-started dev server, per this repo's established convention of running the full E2E suite only in CI).
- [x] 4.4 Search the repo for any remaining consumer of `WorkflowSteps` (the component) or `graviScanSteps`/`WorkflowStep` (the export/interface) besides `Home.tsx` and `WorkflowSteps.tsx` itself: `grep -rn "WorkflowSteps\|graviScanSteps\|\bWorkflowStep\b" src/ tests/` (use a word-boundary pattern for `WorkflowStep` — a plain substring match also hits `DailyWorkflowStep`, the unrelated interface name `CylinderScanWorkflowGuide.tsx`/`GraviScanWorkflowGuide.tsx` each define for their own step data, which is not a `WorkflowSteps.tsx` consumer at all). **Ran it: only doc-comment mentions in `CylinderScanWorkflowGuide.tsx`/`.test.tsx` and `GraviScanWorkflowGuide.tsx`/`.test.tsx`, plus `WorkflowSteps.tsx`/`WorkflowSteps.test.tsx` themselves — no real consumer found.** Updated all four doc comments to drop the now-stale "which continues to render GraviScan's flat list unchanged" framing. Deleted `src/renderer/components/WorkflowSteps.tsx` and `tests/unit/components/WorkflowSteps.test.tsx` entirely.
- [x] 4.5 Run `npm run lint && npx tsc --noEmit && npm run test:unit`; fix any fallout. Lint/tsc clean. Full suite: 1954 passed (net -2 from the deleted `WorkflowSteps.test.tsx`'s 2 tests), 115 skipped, same 2 pre-existing baseline failures. **Commit checkpoint.**

## 5. Manual Visual Verification (Critical Care Point)

- [ ] 5.1 Via the `dev`/`run` workflow, manually verify in a real running Electron window: CylinderScan mode's sidebar (new order, new colors) and Home page; GraviScan mode's sidebar (new order, new colors) and Home page's new `GraviScanWorkflowGuide` (Daily Workflow/Setup sections, Configure Scanner's copy, correct navigation on click for every link); the default/no-mode state (machine-config first-run redirect) is unaffected. This check confirms real-browser rendering matches the `lime-800`/`bg-stone-100`/`bg-stone-200` contrast pairing already computed and documented in `design.md` (≈5.64:1 active, ≈6.49:1 hover) — it is confirmation of a known-good computed choice, not the only check for it. If this sandboxed session cannot open a display, say so explicitly rather than claiming this step done — flag it for the user to complete, matching this repo's established precedent (Tier 4 §4.6) for genuinely undoable manual-verification steps.

## 6. Spec & Full Validation

- [ ] 6.1 Run `npx openspec validate fix-layout-sidebar-nav-parity --strict`; resolve any issues.
- [ ] 6.2 Run the full suite: `npm run lint`, `npx tsc --noEmit`, `npm run test:unit` (`npx vitest run`) — confirm zero regressions beyond whatever pre-existing baseline was established in task 1.4. Record the exact baseline (file names, not just a count) so a reviewer can distinguish this change's fallout from inherited noise.
- [ ] 6.3 Re-run the open-PR file-diff check from proposal scoping (`gh pr list --state open --json number,title,files,updatedAt`, filtered for `Layout.tsx`/`WorkflowSteps.tsx`) immediately before merge, not just at proposal time. **Filter by recency, not just file match**: round-1 review found 15 open PRs (#146-#227) match on file diff alone, but all are stale branches last touched April-May 2026, unrelated to this change — a plain file-match re-run will resurface the same 15 false positives every time. Check each match's `updatedAt` against "has this been touched since #289/#290 merged (2026-08-25/2026-08-31)?" — only a genuinely recently-active match is a real collision risk worth pausing for.

## 7. Team Communication and Follow-Up Filing

- [ ] 7.1 Before or at merge, post a brief heads-up to the team (Slack, matching the existing precedent of using team channels for other GraviScan/CylinderScan operational changes) that the sidebar link order changed in both scan modes — mitigates the muscle-memory disruption round-1 review flagged for operators who click this sidebar by habit under real lab time pressure.
- [ ] 7.2 File a follow-up GitHub issue: `ConfigureScanner.tsx`'s "Reset USB" (`handleResetUsb`) and per-scanner "Remove" (`handleRemove`) actions have no confirmation-dialog guard (confirmed via direct grep — no `window.confirm`/"Are you sure" anywhere in the file). Found during this change's round-1 review, which increases how often Daily Workflow placement prompts operators toward this page. Out of scope for this Layout.tsx/workflow-guide-only change (would require editing `ConfigureScanner.tsx`'s own behavior) — reference this change's PR/issue for context.
