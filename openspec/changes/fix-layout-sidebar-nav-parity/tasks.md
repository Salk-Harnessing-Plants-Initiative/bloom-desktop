## 1. Shell Background and Sidebar Nav-Link Recolor (#328 piece 1)

- [ ] 1.1 Write failing tests in `tests/unit/pages/Layout.test.tsx` (extending its existing `renderLayout` helper): the outer shell `<div>` (`Layout.tsx:432`) has `bg-stone-100`, not `bg-gray-50` — assert via `container.querySelector` or a role-based query on the shell wrapper, in both `cylinderscan` and `graviscan` mode. Add a `describe('Layout nav-link colors')` block asserting: an active nav link (`NavLink`'s `aria-current="page"` or `isActive` render, e.g. render with `initialPath` matching a link's route) has `bg-stone-200`, `text-lime-700`, and `border-r-4 border-lime-700` in its className; no link anywhere has any `blue-*` class. Confirm all new assertions fail against current code (`Layout.tsx:432` bare `bg-gray-50`; `:448-450` blue hover/active classes).
- [ ] 1.2 Implement: `Layout.tsx:432` `bg-gray-50` → `bg-stone-100`. `Layout.tsx:448` `hover:bg-blue-50 hover:text-blue-600` → `hover:bg-stone-100 hover:text-lime-700`; `:450` `bg-blue-50 text-blue-600 border-r-4 border-blue-600` → `bg-stone-200 text-lime-700 border-r-4 border-lime-700`. Confirm 1.1 passes.
- [ ] 1.3 Run `npm run lint && npx tsc --noEmit && npm run test:unit`; fix any fallout. Note the current baseline of pre-existing failing files (there may be none, or a different set than Tier 4's — confirm directly, don't assume Tier 4's list still applies) before proceeding. **Commit checkpoint.**

## 2. Sidebar Link Restructuring and Ordering (#337, both modes)

- [ ] 2.1 Write failing tests in `tests/unit/pages/Layout.test.tsx`'s existing `describe('Layout nav links')` block: in `cylinderscan` mode, all nine links appear in DOM order Home, Camera Settings, Capture Scan, Browse Scans, Export Scans, Scientists, Phenotypers, Accessions, Experiments (use `screen.getAllByRole('link')` and assert the `name`/`href` sequence, not just presence — presence is already covered by existing tests). In `graviscan` mode, all eight links appear in order Home, Configure Scanner, Capture Scan, Browse GraviScans, Scientists, Phenotypers, Metadata, Experiments. Add a regression-guard test that the default/no-mode order (`mode={null}`) is unchanged: Home, Scientists, Phenotypers, Experiments, Browse Scans, Export Scans. Confirm all three fail against current code (verified current orders are in `design.md`).
- [ ] 2.2 Implement in `Layout.tsx`: replace `alwaysLinks` (`:56-137`) with a standalone `homeLink` (Home only) and `setupLinks` (Scientists, Phenotypers, Experiments — same three link objects, order preserved). Move the `Accessions` link object out of `captureLinks` (`:239-257`) into a new CylinderScan-specific setup addition. Move the `Metadata` link object out of `graviscanLinks` (`:302-321`) into a new GraviScan-specific setup addition. Update the `links` composition (`:385-389`) to:
  - cylinderscan: `[homeLink, cameraSettingsLink, captureScanLink, browseScansLink, exportScansLink, ...setupLinks, accessionsLink]`
  - graviscan: `[homeLink, configureScannerLink, captureScanLink, browseGraviscansLink, ...setupLinks, metadataLink]`
  - default/no-mode: `[homeLink, ...setupLinks, browseScansLink, exportScansLink]` (same links as today, reassembled from the new pieces — must render in the same order as before)

  Keep each link's route/label/icon identical to today's — this task reorders and regroups object literals, it does not change any link's content. Confirm 2.1 passes.
- [ ] 2.3 Run `npm run lint && npx tsc --noEmit && npm run test:unit`; fix any fallout. **Commit checkpoint.**

## 3. GraviScanWorkflowGuide Component (#328 piece 2)

- [ ] 3.1 Write a new `tests/unit/components/GraviScanWorkflowGuide.test.tsx`, mirroring `tests/unit/components/CylinderScanWorkflowGuide.test.tsx`'s structure exactly (same `vi.mock('react-router-dom', ...)` pattern, same `mockNavigate` assertions). Cover:
  - A "Daily Workflow" section renders before a "Setup" section (`compareDocumentPosition`).
  - Daily Workflow contains, in order, Configure Scanner, Capture Scan (marked/rendered as the primary large CTA), Browse GraviScans — with no step-number badge.
  - Setup contains Scientists, Phenotypers, Metadata, Experiments as unordered cards with no step-number badge.
  - `data-testid`s use the same `workflow-step-${id}` slug scheme as `CylinderScanWorkflowGuide` (e.g. `workflow-step-configure-scanner`, `workflow-step-capture-scan`), and clicking one navigates to its route: `/configure-scanner`, `/capture-scan`, `/browse-graviscans`, `/scientists`, `/phenotypers`, `/metadata`, `/experiments` (all unchanged from today's `graviScanSteps` routes, except Configure Scanner, which is newly represented here for the first time).
  - Configure Scanner's card description text reflects its "verify before session" role (e.g. "Verify all scanners are detected and connections are healthy before starting a session") — assert this exact copy so a future edit can't silently drop the framing this change's design.md specifically calls for.
  - The Daily Workflow primary card (Capture Scan) uses `bg-lime-700 text-white hover:bg-lime-800`; non-primary cards use `hover:bg-lime-50`.
  - Confirm it fails (component doesn't exist yet).
- [ ] 3.2 Implement `src/renderer/components/GraviScanWorkflowGuide.tsx`, copying `CylinderScanWorkflowGuide.tsx`'s structure (its `DailyWorkflowStep` interface shape, card markup, and lime styling) with GraviScan's own step data: Daily Workflow = Configure Scanner (`/configure-scanner`, "Verify all scanners are detected and connections are healthy before starting a session"), Capture Scan (`/capture-scan`, primary), Browse GraviScans (`/browse-graviscans`); Setup = Scientists (`/scientists`), Phenotypers (`/phenotypers`), Metadata (`/metadata`), Experiments (`/experiments`). Confirm 3.1 passes.
- [ ] 3.3 Run `npm run lint && npx tsc --noEmit && npm run test:unit`; fix any fallout. **Commit checkpoint.**

## 4. Wire GraviScanWorkflowGuide into Home; Retire graviScanSteps

- [ ] 4.1 Update `tests/unit/pages/Home.test.tsx`: replace the `'renders GraviScan workflow steps when mode is graviscan'` test (`:87-101`) with an assertion that `GraviScanWorkflowGuide`'s "Daily Workflow"/"Setup" sections render (mirroring the cylinderscan-mode test at `:66-85`) instead of the generic "Workflow Steps" heading; replace the `'GraviScan step 5 (Capture Scan) navigates to /capture-scan'` test (`:120-133`), which clicks the old numeric `workflow-step-5` testid, with a click on `workflow-step-capture-scan` (mirroring how Tier 4 retired CylinderScan's numeric testids in favor of `workflow-step-scientists`, etc.). Keep every other existing assertion in this file (Today's Activity, config-exists redirect, cylinderscan-mode tests) untouched. Confirm the new/changed assertions fail against current code first.
- [ ] 4.2 Update `Home.tsx`: replace the `mode === 'graviscan'` branch (`:127-136`, currently `<><h2>Workflow Steps</h2><WorkflowSteps steps={graviScanSteps} /></>`) with `<GraviScanWorkflowGuide />`, removing the now-redundant outer "Workflow Steps" heading (GraviScanWorkflowGuide has its own "Daily Workflow"/"Setup" sub-headers, same reasoning Tier 4 applied to the CylinderScan branch). Remove the now-unused `WorkflowSteps`/`graviScanSteps` import (`:4`); add the `GraviScanWorkflowGuide` import. Confirm 4.1 passes.
- [ ] 4.3 Search the repo for any remaining consumer of `WorkflowSteps` (the component) or `graviScanSteps`/`WorkflowStep` (the export/interface) besides `Home.tsx` and `WorkflowSteps.tsx` itself (`grep -rn "WorkflowSteps\|graviScanSteps\|WorkflowStep" src/ tests/`). If none exist, delete `src/renderer/components/WorkflowSteps.tsx` and its test file `tests/unit/components/WorkflowSteps.test.tsx` entirely (not just the `graviScanSteps` export, since nothing else uses the component or interface after this change). If something unexpected still imports it, stop and re-scope this task rather than deleting speculatively.
- [ ] 4.4 Run `npm run lint && npx tsc --noEmit && npm run test:unit`; fix any fallout. **Commit checkpoint.**

## 5. Manual Visual Verification (Critical Care Point)

- [ ] 5.1 Via the `dev`/`run` workflow, manually verify in a real running Electron window: CylinderScan mode's sidebar (new order, new colors) and Home page; GraviScan mode's sidebar (new order, new colors) and Home page's new `GraviScanWorkflowGuide` (Daily Workflow/Setup sections, Configure Scanner's copy, correct navigation on click for every link); the default/no-mode state (machine-config first-run redirect) is unaffected. Confirm legibility of the lime-on-stone hover/active states in both light conditions the app is normally viewed in. If this sandboxed session cannot open a display, say so explicitly rather than claiming this step done — flag it for the user to complete, matching this repo's established precedent (Tier 4 §4.6) for genuinely undoable manual-verification steps.

## 6. Spec & Full Validation

- [ ] 6.1 Run `npx openspec validate fix-layout-sidebar-nav-parity --strict`; resolve any issues.
- [ ] 6.2 Run the full suite: `npm run lint`, `npx tsc --noEmit`, `npm run test:unit` (`npx vitest run`) — confirm zero regressions beyond whatever pre-existing baseline was established in task 1.3. Record the exact baseline (file names, not just a count) so a reviewer can distinguish this change's fallout from inherited noise.
- [ ] 6.3 Confirm no open PR other than this one touches `Layout.tsx`/`WorkflowSteps.tsx` at merge time (re-run the same open-PR file-diff check done during scoping) — re-verify immediately before merge, not just at proposal time, since new PRs may have opened in the interim.
