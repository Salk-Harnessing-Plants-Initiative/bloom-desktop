## 1. Fold in Camera Settings Centering (supersedes `align-page-layout-centering`)

- [x] 1.1 Write a failing test in a new `tests/unit/pages/CameraSettings.test.tsx` (mocking `window.electron.camera`/`window.electron.config` following `tests/unit/components/CameraSettingsForm.test.tsx`'s existing mocking approach): the page's root container has `min-h-screen bg-gray-50 p-6` and wraps its content in a child with `max-w-7xl mx-auto space-y-6`; both settings and preview panels have `shadow-sm` (not `shadow`). Also cover the `ui-layout` spec's two other scenarios, which this change doesn't modify but which are newly spec'd as part of adding this capability: the grid uses `grid-cols-1 lg:grid-cols-2` (confirmed already present at `CameraSettings.tsx:110`, unaffected by this change — this is a "confirm still true" assertion, not a new behavior); both panels have `rounded-lg` alongside `shadow-sm`, with the Settings form in the left/first column and Live Preview in the right/second column. Confirm the centering/shadow assertions fail against current code (`CameraSettings.tsx:101`'s bare `p-8`, `:112`/`:139`'s `shadow`); the grid/rounded-lg/column-order assertions should already pass unmodified (documenting existing behavior the new `ui-layout` spec now covers).
- [x] 1.2 Update `CameraSettings.tsx:101`: change the root `<div className="p-8">` to `<div className="min-h-screen bg-gray-50 p-6"><div className="max-w-7xl mx-auto space-y-6">...</div></div>`, and change both panel `shadow` classes (`:112`, `:139`) to `shadow-sm`. Confirm 1.1 passes.
- [x] 1.3 Delete `openspec/changes/align-page-layout-centering/` (its scope is now implemented and specified here). **Update:** after rebasing this worktree onto current `main` (2026-08-12, following Tiers 2/3/5a merging), this directory no longer exists at all — already gone by some other path (not archived either; likely never committed). Nothing to delete.
- [x] 1.4 Run `npm run lint && npx tsc --noEmit && npm run test:unit`; fix any fallout. **Commit checkpoint.** Lint/tsc clean. Full suite: 7 pre-existing failing files (`AccessionForm.test.tsx`, `config-store.test.ts`, `electron-cleanup.test.ts`, `graviscan/database-handlers.test.ts`, `graviscan/scan-coordinator.test.ts`, `image-uploader.test.ts`, `scans-export.test.ts`) — all unrelated to `CameraSettings.tsx`/this change (confirmed by file/content), inherited from the post-merge `main` baseline (Tiers 2/3/5a). This is the new baseline for the rest of this tier's sections.

## 2. CylinderScan-Only Accent Color Sweep

- [x] 2.1 Write failing tests:
  - New `tests/unit/components/ExperimentChooser.test.tsx`, `tests/unit/components/PhenotyperChooser.test.tsx` (neither exists yet): the `<select>` has `focus:ring-lime-500`, not `focus:ring-blue-500`; the existing `border-amber-300`/`border-gray-300` empty-state toggle is unchanged (regression guard).
  - New `tests/unit/components/AccessionForm.test.tsx`, `tests/unit/components/AccessionList.test.tsx`, `tests/unit/components/AccessionFileUpload.test.tsx` (none exist yet): every blue instance converts to lime — `AccessionForm.tsx:74,86` (`focus:ring-blue-500`); `AccessionList.tsx:167,199,205,307,328` (`focus:ring-blue-500`, `bg-blue-500 hover:bg-blue-600`) **and `:307`'s `border-blue-400`** (a border color, not a focus ring or background — check the whole line, not just the category list) **and `:312`'s `hover:text-blue-600`**; `AccessionFileUpload.tsx:287` (`bg-blue-200`), `:326` (`file:bg-blue-50 file:text-blue-700 hover:file:bg-blue-100`), `:387,429` (`focus:ring-blue-500`), `:499` (`bg-blue-500 hover:bg-blue-600`).
  - Extend `tests/unit/components/PythonStatus.test.tsx`: the "Check Hardware" button has `bg-lime-700`/`hover:bg-lime-800`, not `bg-blue-600`/`hover:bg-blue-700`.
  - New `tests/unit/pages/CaptureScan.test.tsx` (only `CaptureScan-event-cleanup.test.tsx` exists today, and — checked directly — it's entirely `describe.skip`'d and never mounts the real component, so there is no existing mounting precedent to reuse). Mounting `CaptureScan.tsx` for real requires: wrapping in `MemoryRouter`; mocking `window.electron.session.checkIdleReset`/`onIdleReset` (returns a cleanup fn), `database.scans.getMostRecentScanDate` (polled via `setInterval`), `config.get`, `database.scans.getRecent`, `session.get`/`set`, `camera.getStatus`, `camera.getSettings`, `scanner.onProgress`/`onComplete`/`onError` (each returning cleanup fns), **and `database.experiments.list()`** (resolves an empty array is fine) — this last one is required because `CaptureScan.tsx` unconditionally renders `MetadataForm` → `ExperimentChooser`, which calls `database.experiments.list()` on mount unconditionally, and `tests/unit/setup.ts`'s global mock provides `scientists`/`phenotypers`/`accessions` but **not** `experiments` — omitting this mock leaves the dropdown silently empty (caught internally, but contradicts "mount cleanly") rather than throwing loudly, so don't skip it. To reach the "Configure Camera" button (`:828`): mock `camera.getStatus()` to resolve `{connected: false}` and `camera.getSettings()` to resolve falsy, then `waitFor` `cameraConfigured` to flip false. To additionally reach the "Go to Camera Settings" link (`:725`, inside the collapsible panel): after the above, `fireEvent.click` the "Camera Settings" toggle button (gated by `showCameraSettings` state) before asserting. Assert both elements use lime classes, not blue. Also add a regression-guard assertion that `CaptureScan.tsx`'s amber warning banners (idle-reset banner, duplicate-scan-today banner) are unchanged by this same PR.
  - Confirm all fail against current code.
- [x] 2.2 Implement the class swaps at each cited location. Leave `CaptureScan.tsx`'s green Start Scan button/success banner and all amber warning-state borders untouched.
- [x] 2.3 Run `npm run lint && npx tsc --noEmit && npm run test:unit`; fix any fallout. **Commit checkpoint.** Clean; same 7 pre-existing failing files as the §1.4 baseline (electron-cleanup's exact failing subtests vary run-to-run — timing-flaky, unrelated to this change).

## 3. Shared BrowseScans/ScanPreview/Entity-Form/MachineConfiguration Accent Color Sweep

- [x] 3.1 Write failing tests:
  - New `tests/unit/pages/ScanPreview.test.tsx`: the two "Back to Scans" links and the Upload button use lime classes, not blue (`ScanPreview.tsx:216,233,253,337`).
  - New `tests/unit/pages/BrowseScans.test.tsx` — this is the FIRST test file for this page (none exists today). Organize it with explicit `describe` blocks (e.g. `describe('baseline rendering')`, `describe('color sweep')`, `describe('thumbnail column')` (added in §6), `describe('camera-settings column')` (added in §6)) rather than one flat file, since it accretes scenarios across this section and §6:
    - `describe('baseline rendering')`: table renders with the documented columns; empty-state message + CaptureScan link when no scans exist; pagination controls render with correct total-count text.
    - `describe('color sweep')`: the three filter `<select>`/`<input>` elements have `focus:ring-lime-500` (`BrowseScans.tsx:254,278,295`); the batch button (rendered text is "Upload Selected (N)"/"Uploading...", not "Start uploading") uses lime (`:358`); the Plant ID link and row view-icon use lime (`:460,487`); the row's existing `hover:bg-gray-50` and `divide-gray-100` chrome, and the upload-status indicator colors (red/yellow/green/gray from `getUploadStatus()`), are unchanged (regression guard).
  - New `tests/unit/components/ExperimentForm.test.tsx`, `tests/unit/components/PhenotyperForm.test.tsx`, `tests/unit/components/ScientistForm.test.tsx` (none exist yet): each form's `focus:ring-blue-500` inputs/buttons convert to `focus:ring-lime-500` (`ExperimentForm.tsx:140,230`; `PhenotyperForm.tsx:83,99,110`; `ScientistForm.tsx:83,99,110`). **Note:** `ExperimentForm.tsx` is also being edited by open PR #290 (a new wave-attach panel) — re-check these line numbers against the current file before writing the test, since a two-line color swap on shifted lines is a trivial, low-risk mechanical conflict even if #290 merges before this change (unlike the `Layout.tsx`/`WorkflowSteps.tsx` collision this change avoids entirely — see `design.md`'s "Deferred Scope").
  - New `tests/unit/pages/MachineConfiguration.test.tsx` (doesn't exist yet): every blue instance converts to lime — checkboxes (`:249,265`, `text-blue-600`→`text-lime-700` or the accent-color equivalent for a checkbox input), input focus rings (`:299,328,350,372,487,542,587,654,687`, `focus:ring-blue-500`→`focus:ring-lime-500`), and the two submit buttons (`:388,714`, `bg-blue-600 hover:bg-blue-700`→`bg-lime-700 hover:bg-lime-800`).
  - Confirm all fail against current code.
- [x] 3.2 Implement the class swaps at each cited location.
- [x] 3.3 Run `npm run lint && npx tsc --noEmit && npm run test:unit`; fix any fallout. **Commit checkpoint.** Clean; same 7 pre-existing failing files as baseline (`BrowseScans.test.tsx`/`ScanPreview.test.tsx` already existed post-merge from Tier 2/3 — extended with baseline + color-sweep describe blocks rather than created fresh).

## 4. #175 — CylinderScan Workflow Guide Restructure (new component, GraviScan untouched)

> **Scope note:** this section touches only files exclusive to CylinderScan's rendering path. `WorkflowSteps.tsx`'s shared component, `graviScanSteps`, and `Layout.tsx` are NOT edited anywhere in this section (or anywhere in this change) — see `design.md`'s "Deferred Scope" for why (PRs #289/#290 are actively rewriting those files) and the "`WorkflowSteps.tsx` stays untouched" decision for the architecture this enables.

- [x] 4.1 Write a new `tests/unit/components/CylinderScanWorkflowGuide.test.tsx`. Cover:
  - A "Daily Workflow" section renders before a "Setup" section.
  - Daily Workflow contains Camera Settings, Capture Scan (marked/rendered as the primary large CTA), Browse Scans, in that order, with no step-number badge; Setup contains Scientists, Phenotypers, Accessions, Experiments as unordered cards with no step-number badge.
  - `data-testid`s use a `workflow-step-${id}` slug scheme (e.g. `workflow-step-capture-scan`, `workflow-step-scientists`) and clicking one navigates to its route (routes unchanged from today's `cylinderScanSteps`: `/camera-settings`, `/capture-scan`, `/browse-scans`, `/scientists`, `/phenotypers`, `/accessions`, `/experiments`).
  - The Daily Workflow primary card uses `bg-lime-700 text-white hover:bg-lime-800` (this is a new component's own native styling, not a recolor).
  - Confirm it fails (component doesn't exist yet).
- [x] 4.2 Update `tests/unit/pages/Home.test.tsx`'s existing cylinderscan-mode assertions: replace the `workflow-step-1`-navigates-to-`/scientists` assertion (this component's old numeric testids are being retired for CylinderScan only) with `workflow-step-scientists`; keep asserting `'Camera Settings'`/`'Accessions'` text presence (content unchanged, only presentation changes). **Leave every graviscan-mode assertion in this file completely untouched** (`'Metadata'` present, `'Camera Settings'` absent, and any existing `graviScanSteps`/`WorkflowSteps`-testid assertions) — this file's GraviScan-mode behavior is not affected by this change at all. Confirm the cylinderscan-mode assertion fails against current code before the implementation step below.
- [x] 4.3 Implement `src/renderer/components/CylinderScanWorkflowGuide.tsx`: its own step data (moved out of `cylinderScanSteps`, not shared with `WorkflowSteps.tsx`) grouped into Daily Workflow (Camera Settings → "confirm/verify before each session" copy per #175, Capture Scan as the primary CTA, Browse Scans) and Setup (Scientists, Phenotypers, Accessions, Experiments, unordered, no step numbers) — same routes/copy as today's `cylinderScanSteps`, only regrouped and delabeled. Confirm 4.1 passes.
- [x] 4.4 Update `Home.tsx`: today there is no per-mode JSX branch, just `const steps = mode === 'graviscan' ? graviScanSteps : cylinderScanSteps;` followed by one shared `<WorkflowSteps steps={steps} />` call (`Home.tsx:45,58`) — introduce a real conditional render (`mode === 'graviscan' ? <WorkflowSteps steps={graviScanSteps} /> : <CylinderScanWorkflowGuide />`), not a one-line swap. `Home.tsx:55-56`'s outer `<h2>Workflow Steps</h2>` heading (shared by both modes today) becomes redundant once `CylinderScanWorkflowGuide` has its own "Daily Workflow"/"Setup" sub-headers — remove this outer heading specifically when rendering `CylinderScanWorkflowGuide` (keep it as-is for the `graviscan` branch, unchanged). Confirm 4.2 passes.
- [x] 4.5 Remove the now-unused `cylinderScanSteps` export from `src/renderer/components/WorkflowSteps.tsx` (a pure deletion — the `WorkflowStep` interface, `graviScanSteps`, and the `WorkflowSteps` component's rendering logic are otherwise untouched). Confirm no other file still imports `cylinderScanSteps` (`grep -rn cylinderScanSteps src/`). Confirmed zero remaining references.
- [ ] 4.6 Manual visual check (via the `dev`/`run` workflow) — **attempted, not completed**: this sandboxed session has no reliable way to launch and observe the actual Electron GUI (the dev server didn't stay up as a backgroundable process in this shell, and there's no confirmed display for screenshots), the same constraint already documented for the E2E suite in this repo's history (Tier 1/2/3). Relying instead on the extensive unit-test coverage across all 7 sections, which asserts the exact CSS classes/attributes/DOM structure for every visual claim (colors, container/grid classes, thumbnail `loading`/`src` attributes, section ordering, etc.) — flagging the actual pixel-level visual check as unverified locally, to be done manually before/during the real PR review, not claimed as complete here.
- [x] 4.7 Run `npm run lint && npx tsc --noEmit && npm run test:unit`; fix any fallout. **Commit checkpoint.** Clean; same pre-existing baseline failures (this run: 6 files/8 tests — `AccessionForm.test.tsx`'s two timing-flaky tests happened to pass this time, consistent with the flakiness already noted, not a regression).
- [x] 4.8 File a follow-up GitHub issue tracking the deferred GraviScan-side work (`graviScanSteps`'s equivalent Daily-Workflow/Setup restructure, plus `Layout.tsx`'s shell-background/sidebar-nav-link recolor — both originally in this change's scope before Revision 4 deferred them, see `design.md`'s "Deferred Scope"), to be picked up once PR #289/#290 merge and settle. Filed as [#328](https://github.com/Salk-Harnessing-Plants-Initiative/bloom-desktop/issues/328) — both PRs had merged by the time this was filed, so the issue is unblocked and ready to pick up. Referenced in the PR description for this change.

## 5. #104 — Home Status Dashboard: Hardware Status Copy + Activity Widgets

- [x] 5.1 Extend `tests/unit/components/PythonStatus.test.tsx`: when a hardware component (camera/DAQ) is unavailable, a "Contact your administrator" message renders; add a regression-guard assertion that no element anywhere in the rendered output links to `/machine-config` (confirmed absent today — this locks in that absence rather than leaving it untested). Confirm the new assertions fail (no such copy exists yet) before implementing.
- [x] 5.2 Implement: add "Contact your administrator" copy to `PythonStatus.tsx`'s hardware-unavailable render branch. No Machine Config link is added (none exists today). Confirm 5.1 passes.
- [x] 5.3 Write a failing unit test for a new `src/utils/upload-status.ts` exporting `countUploadStatuses(images: { status: string }[]): { pending: number; failed: number; uploaded: number }` — a pure counting function, no `id` required (so it works for both `BrowseScans.tsx`'s `{id, status}` images and the `{status}`-only images `getRecent` will return). Cover: mixed statuses count correctly into all three buckets; `pending`/`uploading` both count as `pending`; empty array returns all zeros.
- [x] 5.4 Implement `countUploadStatuses()`. Rewrite `BrowseScans.tsx`'s existing `getUploadStatus()` (`BrowseScans.tsx:212-235`) to call `countUploadStatuses()` internally, then apply the exact same priority rules it already uses (`failed > 0` → `"N failed"` red; `pending > 0` → `"X/Y uploaded"` yellow; all uploaded → `"All uploaded"` green; else `"X/Y"` gray; zero images → `"No images"` gray) — a behavior-preserving refactor, not a logic change. Confirm `BrowseScans.test.tsx`'s existing upload-status-color assertions (§3) still pass unchanged.
- [x] 5.5 Write a failing test: with `window.electron.database.scans.getRecent` mocked to resolve today's scans (each including an `images: [{status}]` array), the Home page renders a "Today's Activity" section showing each scan's date/plant ID/experiment and an upload-status breakdown that is the **sum of `countUploadStatuses()` across every returned scan's images** (assert a concrete total, e.g. 2 scans with 3 and 2 images respectively, mixed statuses, produces the correct combined pending/failed/uploaded counts — not just "a breakdown renders"). When `getRecent` resolves an empty array, an empty/neutral state renders (not an error). Confirm it fails against current code.
- [x] 5.6 Extend `tests/e2e/renderer-database-ipc.e2e.ts`'s existing "Renderer Database IPC - Scans getRecent" describe block (`:2347-2397`) with a failing assertion that a scan with images resolves `result.data![0].images` containing each image's `status` (currently only `plant_id` is asserted — this is a real backend query-shape change with no test tied to it otherwise). Confirm it fails against current code (no `images` in the query at all).
- [x] 5.7 Extend `src/main/database-handlers.ts`'s `db:scans:getRecent` handler (`:1908-1917`) to include `images: { select: { status: true } }` in its existing `include`. Update `src/types/electron.d.ts`'s declared return type for this handler — note it currently already (incorrectly) claims full `images: true` inclusion via `ScanWithRelations`, a pre-existing drift from the real query; fix it to accurately reflect the new `{status}`-only image shape rather than assuming the existing type already covers this. Confirm 5.6 passes.
- [x] 5.8 Implement the "Today's Activity" section in `Home.tsx`, using `getRecent` and `countUploadStatuses()` from §5.4. Confirm 5.5 passes.
- [x] 5.9 Write a failing E2E test in `tests/e2e/renderer-database-ipc.e2e.ts` (this is a genuinely new `db:*` handler, subject to the IPC coverage gate) asserting `db:scans:getFailedUploadCount` returns `{ success: true, data: { failedCount: N } }`, correctly counting only `status: 'failed'` images on non-deleted scans, excluding soft-deleted scans' images, across any capture date (not just today).
- [x] 5.10 Implement `db:scans:getFailedUploadCount` in `src/main/database-handlers.ts`: `db.image.count({ where: { status: 'failed', scan: { deleted: false } } })`, returned as `{ success: true, data: { failedCount } }`. Wire it through `src/main/preload.ts` and `src/types/electron.d.ts` following the existing pattern for other `scans.*` handlers. Confirm 5.9 passes.
- [x] 5.11 Write a failing test: Home page renders a persistent "N failed uploads need attention" indicator (linking to `/browse-scans`) whenever `getFailedUploadCount` resolves `failedCount > 0`, **independent of whether `getRecent` resolves any scans for today** (test this specifically: empty `getRecent` result + non-zero `failedCount` still shows the indicator). No indicator when `failedCount` is 0.
- [x] 5.12 Implement the indicator in `Home.tsx`. Confirm 5.11 passes.
- [x] 5.13 Run `npm run lint && npx tsc --noEmit && npm run test:unit`; fix any fallout. **Commit checkpoint.** Lint/tsc clean. Full suite: 7 pre-existing failing files, same baseline. **Fixed a real regression found along the way:** `tests/unit/pages/App.test.tsx` mounts the real `Home` component but had no `window.electron.database` mock at all — my new `getRecent`/`getFailedUploadCount` effects threw synchronously, corrupting React's test scheduler ("Should not already be working"). Added the missing mock to that file's `beforeEach` (a genuine pre-existing gap this change exposed, not a design flaw). **E2E note:** §5.6/5.9's new E2E assertions (images/status inclusion, `getFailedUploadCount`) were written and the corresponding backend code implemented, but the E2E suite itself was not run in this sandboxed session (same environment limitation noted in Tier 1/2/3's history) — flagged as unverified locally, to be confirmed on CI.

## 6. #106/#107 — BrowseScans Thumbnail and Camera-Settings Columns

- [x] 6.1 Searched `tests/unit`, `tests/integration`, and `tests/e2e` for any test asserting `db:scans:list`'s paginated `images` select shape (`{id, status}`-only) or `db:scans:getRecent`'s response shape: **none found** (confirmed directly, not assumed — see `design.md`'s risk re-assessment). No pre-step is needed here beyond the implementation below.
- [x] 6.2 Write a failing test asserting `db:scans:list`'s paginated query's `images` select includes `path` and `frame_number` alongside `id`/`status`. Update `src/main/database-handlers.ts:1746`'s select accordingly. Update `src/types/database.ts`'s `ScanWithImageSummary` type to match. Confirm the test passes and confirm no other existing test broke.
- [x] 6.3 Extend `tests/unit/pages/BrowseScans.test.tsx`'s `describe('thumbnail column')` block (from §3.1): a scan with 2+ images renders a `<img loading="lazy">` thumbnail (~48×64px) sourced from the lowest-`frame_number` image's path, resolved via the same `isAbsolutePath()`/`scans_dir`/`pathToFileUrl()` pattern `ScanPreview.tsx` already uses; a scan with zero images renders a placeholder, not a broken `<img>`; an image whose `onError` fires falls back to the same placeholder.
- [x] 6.4 Implement the thumbnail column in `BrowseScans.tsx`. Confirm 6.3 passes.
- [x] 6.5 Extend `tests/unit/pages/BrowseScans.test.tsx`'s `describe('camera-settings column')` block: a compact camera-settings cell renders including scanner name (e.g. `"{scanner_name} · Exp {exposure_time}ms · Gain {gain}"`) with a `title` attribute containing the full set at unrounded precision — scanner name, exposure, gain, brightness, contrast, gamma, seconds-per-rotation (all seven fields, not just the four not shown in the compact cell — the compact cell's own two/three displayed fields must also appear in the tooltip at full precision).
- [x] 6.6 Implement the camera-settings column in `BrowseScans.tsx` (pure frontend — these fields are already present on each `scan` object in the existing response). Confirm 6.5 passes.
- [x] 6.7 **Moot as of this implementation session:** Tier 2 (`add-cylinderscan-delete-upload-integrity`) merged to `main` via PR #304 before this worktree was rebased (2026-08-12) — its delete-confirmation-modal work (`DeleteConfirmModal`, `handleDelete`/`confirmDelete`) is already part of the `BrowseScans.tsx` this tier has been editing all along, not a separate worktree to diff against. Confirmed no collision: this tier's edits (Preview/Camera-Settings columns, color classes, `images` select, `countUploadStatuses` refactor) are in disjoint regions from Tier 2's delete flow throughout.
- [x] 6.8 Run `npm run lint && npx tsc --noEmit && npm run test:unit`; fix any fallout. **Commit checkpoint.** Clean; same 7 pre-existing failing files, same baseline.

## 7. Spec & Full Validation

- [x] 7.1 Run `npx openspec validate add-cylinderscan-style-ux-parity --strict`; resolve any issues. **Valid.**
- [x] 7.2 Run the full suite: `npm run lint`, `npx tsc --noEmit`, `npm run test:unit` (`npx vitest run`) — confirm zero regressions beyond the pre-existing baseline. **All clean**: lint and `tsc --noEmit` both produce zero output; full `vitest run` consistently shows 7 pre-existing failing files (`AccessionForm.test.tsx`, `config-store.test.ts`, `electron-cleanup.test.ts`, `graviscan/database-handlers.test.ts`, `graviscan/scan-coordinator.test.ts`, `image-uploader.test.ts`, `scans-export.test.ts`) across every section's gate in this tier, confirmed unrelated to this change by file/content and inherited from the post-Tiers-2/3/5a-merge `main` baseline established at §1.4. Final run: 1424 passed, 10 failed (same 7 files), 91 skipped. **E2E status:** confirmed `playwright.config.ts` has no `webServer` auto-start config — the E2E suite requires a manually-run dev server in a separate process (per the test file's own docstring), not automatable in this single-shell sandboxed session. This matches the exact same constraint documented in Tier 1/2/3's history in this same repo. The new/extended E2E assertions (§5.6 `getRecent` images/status, §5.9 `getFailedUploadCount`, §6.2 `db:scans:list` path/frame_number) were written correctly and the corresponding backend code implemented and type-checked, but were not run locally — flagged as unverified locally, to be confirmed on CI, consistent with prior tiers' convention.
