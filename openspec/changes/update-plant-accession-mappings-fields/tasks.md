# Tasks

- [ ] A. Update `genotype_id` → `accession_name` in `usePlateAssignments.ts` (comments + type + field access)
- [ ] B. Update `genotype_id` → `accession_name` in `GraviScan.tsx` (type + field access)
- [ ] Verify: `npx tsc --noEmit` passes
