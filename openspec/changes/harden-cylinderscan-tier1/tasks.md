## 1. Single Instance Application Lock (#249)

- [x] 1.1 Write a failing unit test (new file `tests/unit/single-instance-lock.test.ts`) for a small extracted helper, e.g. `shouldQuitAsSecondInstance(hasLock: boolean): boolean` and a `focusExistingWindow(win: BrowserWindow | null)` helper — asserting: `shouldQuitAsSecondInstance(false)` returns `true` (lock not acquired → quit); `shouldQuitAsSecondInstance(true)` returns `false`; `focusExistingWindow` calls `restore()` when the window `isMinimized()`, and always calls `focus()`; **`focusExistingWindow(null)` does not throw and is a no-op** (covers the case where `second-instance` fires before `createWindow()` has run). Confirm it fails (helpers don't exist yet). Extracting these as small pure/near-pure functions makes the startup logic testable without spinning up a real second Electron process.
- [x] 1.2 Implement the helpers in `src/main/main.ts` (or a small new `src/main/single-instance.ts` if that keeps `main.ts` cleaner) and call `app.requestSingleInstanceLock()` immediately after the existing `electron-squirrel-startup` check (`main.ts:69-74`), before any `app.on('ready', ...)` registration: if the lock isn't acquired, call `app.quit()` and return early. Register `app.on('second-instance', () => focusExistingWindow(mainWindow))` (passing the current value of `mainWindow`, which may still be `null`). Confirm 1.1 passes.
- [x] 1.3 Run `npm run lint && npx tsc --noEmit && npm run test:unit`; fix any fallout.

## 2. Preload Listener Cleanup + Mode-Aware Python Backend Status (#96 + #198)

> Combined into one section: both fixes touch `PythonStatus.tsx`'s same `useEffect`, and — per design.md's Rules-of-Hooks finding — the mode gate has to live inside the effect body, not just at render level, so they can't be cleanly separated into two independent passes over this file.

- [x] 2.1 Create `tests/unit/preload-listener-cleanup.test.ts` importing the real `src/main/preload.ts` module (following `tests/unit/preload-scanner.test.ts`'s mocking approach for `ipcRenderer`, but against the real module rather than a hand-mocked mirror). Write failing tests asserting each of the 8 target listeners returns a function, and that calling it results in `ipcRenderer.removeListener` being called with the correct channel name and the same listener reference passed to `ipcRenderer.on`: `pythonAPI.onStatus`, `pythonAPI.onError`, `cameraAPI.onTrigger`, `cameraAPI.onImageCaptured`, `daqAPI.onInitialized`, `daqAPI.onPositionChanged`, `daqAPI.onHome`, `daqAPI.onError`. Also assert **calling the returned cleanup function a second time does not throw** (idempotency), matching this repo's existing convention for double-cleanup safety (see `scanning/spec.md`'s "Double cleanup is safe" pattern for `ScannerSubprocess` worker management). Confirm all fail (no cleanup function currently returned).
- [x] 2.2 Update the 8 listed methods in `src/main/preload.ts` to match the existing `cameraAPI.onFrame` (87-92) pattern: capture the listener in a named const, call `ipcRenderer.on(channel, listener)`, return `() => ipcRenderer.removeListener(channel, listener)` (`removeListener` is naturally idempotent). Confirm 2.1 passes.
- [x] 2.3 Update `src/types/electron.d.ts`: change the 8 target methods' return type on `PythonAPI`/`CameraAPI`/`DAQAPI` from `void` to `() => void`, matching `CameraAPI.onFrame`'s existing signature. Run `npx tsc --noEmit`.
- [x] 2.4 Add failing tests to `tests/unit/pages/Home.test.tsx` (which already mocks `window.electron.python` and tests mode branching for other elements; add a dedicated `PythonStatus.test.tsx` instead if `PythonStatus` isn't already directly unit-tested — check during implementation): with `mode="cylinderscan"`, mounting then unmounting a component that renders `<PythonStatus mode="cylinderscan" />` calls `window.electron.python.getVersion()` and registers `onStatus`/`onError` (assert via mocks that return jest/vitest mock cleanup functions), and unmounting invokes both returned cleanup functions; a status/error event dispatched after unmount does not update any post-unmount state; the "Python Backend Status" heading and Camera/DAQ content are present. With `mode="graviscan"`: **`window.electron.python.getVersion()`/`onStatus`/`onError` are never called at all** (not just "the heading is absent" — assert the mocks themselves received zero calls, to catch the Rules-of-Hooks bug design.md describes), and no "Python Backend Status" heading or Camera/DAQ content renders anywhere. Confirm all fail against current code.
- [x] 2.5 Update `PythonStatus.tsx`: add a `mode` prop to the component signature; gate the existing `useEffect` body itself on `mode === 'cylinderscan'` (i.e. `useEffect(() => { if (mode !== 'cylinderscan') return; /* existing getVersion + onStatus/onError subscription, now capturing and returning both cleanup functions */ }, [mode])`) — the mode check must be inside the effect, not only at the component's render-level return, since hooks must run unconditionally; return `null` from the component's render when `mode !== 'cylinderscan'`. Update `Home.tsx:60-63` to pass `<PythonStatus mode={mode} />` using its already-available `mode` prop. Confirm 2.4 passes.
- [x] 2.6 Run `npm run lint && npx tsc --noEmit && npm run test:unit`; fix any fallout. (Also updated `tests/unit/setup.ts`'s global `mockPythonAPI.onStatus`/`onError` to return cleanup functions — the global test setup had the same stale contract as the per-test mocks, breaking `App.test.tsx` until fixed.)

## 3. IPC Command Response Correlation (#47)

> Commit-safety note (revised after review): the Python-side `id`-echo (3.1-3.4) is additive and backward-compatible on its own (old TS code ignores an extra `id` key), so it's safe to land as its own smaller commit *before* the TS-side change (3.5-3.7) — only the reverse order is unsafe. They don't need to land in the same commit.

- [x] 3.1 Write failing Python tests in `python/tests/test_ipc_handler.py` (or create it if no such file exists; add a fixture that resets the module-level `_current_request_id` to `None` before/after each test to avoid cross-test state leakage): `send_data()` includes the module-level `_current_request_id` as an `"id"` key in its emitted JSON **only when `_current_request_id` is not `None`; the key is omitted entirely (not sent as `null`) when there's no current request id** (round-3 clarification, resolving an earlier ambiguity between "omit" and "send null" that could otherwise silently break `test_send_data`'s existing full-payload-equality assertion — see `design.md`); `send_error(message, tag_request=True)` (default) includes the current id in its emitted payload; **`send_error(message, tag_request=False)` never includes an id, regardless of what `_current_request_id` currently holds** (this is the fix for the streaming-thread bug found during review — see `design.md`). Decide the minimal wire-format change needed to carry an id on error responses (e.g. switching error responses to a `DATA:`-style JSON envelope with `success: false`, matching the existing `except Exception as e: send_data({"success": False, "error": str(e)})` pattern already used in `handle_scanner_command`'s catch-all, rather than a bespoke `ERROR:<id>:<message>` format). Confirm tests fail against current code. **Update the existing `test_send_error` test in this same file** (which currently asserts the exact literal output `"ERROR:test error\n"`) to match whatever wire format is chosen — this existing assertion will otherwise break. **Confirm the existing `test_send_data` test still passes unmodified** once 3.2 is implemented (it does a full dict-equality assertion against `send_data()`'s output; since the `id` key is omitted rather than nulled when unset, this test's no-id case is unaffected — explicitly verify this rather than assuming it, since a "send null" implementation would have silently broken it). **Implementation decision:** kept the `ERROR:` prefix (not routed through `DATA:`) but changed its payload from a bare string to a JSON envelope `{"message": ..., "id"?: ...}` — preserves the existing `main.ts` `pythonProcess.on('error', (error: string) => ...)` consumer's single-string-argument usage (TS side extracts `.message` before re-emitting), rather than the `DATA:`-with-`success:false` alternative sketched here, which would have conflated command-level errors with the `sendCommand()` `data`-event correlation path.
- [x] 3.2 Implement: `handle_command()` sets a module-level `_current_request_id = cmd.get("id")` at the top (before dispatching to `ping`/`get_version`/`check_hardware`/`camera`/`daq`/`scanner` branches), reset to `None` after the command completes (success or exception). `send_data()` reads `_current_request_id` and merges an `"id"` key into its payload only when it's not `None` — never adds `"id": None`. `send_data()` is confirmed to be called only from `handle_command()`'s own synchronous call stack (grep-confirmed it's never called from `streaming_worker()` or any other thread). `send_error()` gains a `tag_request: bool = True` parameter; when `True`, it reads `_current_request_id` (same omit-if-`None` rule); when `False`, it omits the id entirely regardless. Confirm 3.1 passes.
- [x] 3.3 Update `streaming_worker()`'s two `send_error()` call sites (camera-unavailable check, catch-all exception handler) to pass `tag_request=False` explicitly — this is the actual bug fix, not just plumbing: without it, a streaming-thread error fired while an unrelated main-thread command is in flight would incorrectly reject that unrelated command. Add a Python test asserting this: simulate `_current_request_id` being set to some value (as if a command were in flight), call `send_error(msg, tag_request=False)` (as `streaming_worker()` does), and assert the emitted payload carries no id despite `_current_request_id` being non-`None`.
- [x] 3.4 **Correction (per round-2 review): there is no PR #41 delay to remove.** An earlier draft of this task said to remove "a hardcoded ~1-second pre-camera-detection delay landed in PR #41." That claim traces to issue #47's own text, but doesn't match what PR #41 actually shipped (`gh pr view 41`): an "Apply Settings" button gate in `CameraSettings.tsx`/`CameraSettingsForm.tsx` (still live today, and NOT touched by this proposal), not a timing delay — confirmed no such delay exists anywhere in `src/main`, `src/renderer`, or `python/`. No action needed for this task; it exists only as a note so a future reader doesn't go looking for a delay that was never there, or mistakenly touch the Apply-Settings gate thinking it's related.
- [x] 3.5 Write failing TypeScript tests in `tests/unit/python-process.test.ts` (extending the existing file): (a) two overlapping `sendCommand()` calls each resolve with the response matching their own request id, even if responses arrive out of order; (b) an error response carrying a specific id rejects only that pending call, leaving a second concurrent call still pending; (c) an error event with no id rejects every currently-pending call; (d) a response whose id has already timed out and been removed from `pendingRequests` is ignored (no resolve/reject, no error thrown) rather than causing a crash; (e) when the underlying process emits `'exit'`, every currently-pending request rejects immediately (new behavior — see 3.7). **Update the existing test that emits `process.emit('data', { success: true })` with no `id`** — under the new design this payload has no matching pending-request entry and must not resolve any promise; rewrite that test's expectation (or give it a matching id) rather than leaving it to hang. Confirm all new/updated tests fail against current code.
- [x] 3.6 Implement in `src/main/python-process.ts`: add a private incrementing counter and `pendingRequests: Map<number, {resolve, reject, timeoutId}>`; rewrite `sendCommand()` to generate the next id, store its handlers in the map, send `{...command, id}`; replace the per-call `once('data'/'error', ...)` with persistent listeners (registered once, e.g. in the constructor) that look up the incoming payload's `id` in `pendingRequests` and resolve/reject/delete only that entry, or (for an id-less error) reject and clear every entry in the map; ensure a timed-out request's map entry is deleted when its timeout fires. Confirm 3.5(a)-(d) passes.
- [x] 3.7 Extend the existing `process.on('exit', ...)` handler to reject every entry currently in `pendingRequests` (with an "process exited" error) rather than leaving each one to individually time out — a pre-existing reliability gap found during review, cheap to close while this exact code is already being restructured. Confirm 3.5(e) passes.
- [x] 3.8 Add a test exercising the real `saveScanToDatabase()` path (new file `tests/unit/scanner-process-id-correlation.test.ts`) in `scanner-process.ts` (not an isolated function) with a mocked Prisma client: pass a scan result object containing an `id` field (simulating the new IPC response shape) through `saveScanToDatabase()`, and assert the captured Prisma `data:` argument does not contain an `id` key. This tests the actual protection mechanism — `saveScanToDatabase()`'s Prisma call explicitly enumerates fields rather than spreading the response object — rather than an isolated unit that wouldn't catch a future regression if that call site is ever "simplified" to a spread.
- [x] 3.9 Run `npm run lint && npx tsc --noEmit && npm run test:unit && uv run pytest python/tests/test_ipc_handler.py python/tests/test_scanner.py`; fix any fallout. (Also ran the full `python/tests/` and full `npx vitest run` suites — only pre-existing, unrelated failures present: Windows temp-file-lock flakes in 6 Python tests and the same 5 pre-existing JS test files failing as before any Tier 1 work began.)

## 4. Custom Protocol Scheme Replacing `webSecurity: false` (#93)

- [ ] 4.1 Write failing unit tests for a new path-validation helper — `resolveScanPath(requestedPath: string, scansDir: string, pathModule: typeof import('path') = path): string | null` in a new `src/main/scan-protocol.ts` (accepting an **injectable path module**, defaulting to the ambient `path`, so Windows behavior can be tested deterministically on any CI runner — see the round-3 fix below), returning the resolved absolute path if it's contained within `scansDir`, `null` otherwise. Test cases, all passing `path.win32` explicitly for the Windows-specific ones: a path inside `scansDir` resolves correctly (both `path.win32` and `path.posix`); a path containing `..` segments that would escape `scansDir` returns `null`; an absolute-path-override attempt (e.g. a second drive letter, tested via `path.win32`) returns `null`; a same-directory path differing only in case is accepted when `pathModule === path.win32` (case-insensitive containment) and correctly rejected as a different path when `pathModule === path.posix`; **a sibling directory sharing a name prefix (e.g. `scansDir` = `C:\bloom-scans`, requested path resolves into `C:\bloom-scans-archive\x.png`, tested via `path.win32`) returns `null`** — the boundary-aware containment check found missing during round-2 review. Confirm tests fail (helper doesn't exist).
- [ ] 4.2 Implement `resolveScanPath()` using the **injected** `pathModule`'s `.resolve()` and `.sep` consistently for every operation — resolution, the boundary-aware containment check (`resolved === scansDir || resolved.startsWith(scansDir + pathModule.sep)`, not a bare `startsWith(scansDir)`, and not the ambient `path.sep`), and the case-lowering step (applied when `pathModule === path.win32`, not via a separate `process.platform` check) — never mixing the injected module for some operations and the ambient `path` for others. **Round-3 fix:** the real registered `bloom-scan://` handler in `main.ts` (task 4.5) calls `resolveScanPath()` with its default `pathModule` argument (the ambient `path`), which correctly resolves to `path.win32` when the app is actually running on Windows — only the *tests* need to inject `path.win32`/`path.posix` explicitly to get deterministic behavior regardless of the CI runner's host OS; production code doesn't need to know or care which platform it's on. Confirm 4.1 passes.
- [ ] 4.3 Write failing unit tests **rewriting** `src/utils/file-url.ts`'s existing test coverage (find or create `tests/unit/file-url.test.ts` — if it exists, its current ~8 cases all assert `file://` output and must be rewritten, not left alongside new ones): `pathToFileUrl()` now emits `bloom-scan://` instead of `file://`, preserving the existing backslash-normalization, drive-letter, and percent-encoding behavior (e.g. `C:\Users\foo\bar.png` → `bloom-scan:///C:/Users/foo/bar.png`). Confirm it fails against current `file://`-emitting code.
- [ ] 4.4 Update `pathToFileUrl()` in `src/utils/file-url.ts` to emit the `bloom-scan://` scheme. Confirm 4.3 passes.
- [ ] 4.5 In `src/main/main.ts`: call `protocol.registerSchemesAsPrivileged([{ scheme: 'bloom-scan', privileges: { standard: true, secure: true, supportFetchAPI: true, corsEnabled: true } }])` at module load time (before `app.on('ready', ...)`); inside the existing `'ready'` handler, register `protocol.handle('bloom-scan', (request) => { ... })` **before** the existing `createWindow()`/`loadURL()` call (not after), reading the currently-configured `scans_dir` fresh from the config-store accessor on every request (not a value captured once at registration time) and calling `resolveScanPath()` to serve the file or reject anything outside it. Remove `webSecurity: false` from the `BrowserWindow` `webPreferences`. This has no dedicated new unit test beyond 4.1/4.2 (protocol registration itself is best verified by the E2E test in 4.7) but confirm `npx tsc --noEmit` passes and no other test regresses.
- [ ] 4.6 Write a failing test asserting the "reads scans_dir fresh" property specifically (not just via 4.1/4.2's static-argument tests): call the protocol-handling logic once with `scans_dir` = A, then again after changing the config-store's value to `scans_dir` = B, and assert the second call resolves against B, not a value cached from the first call. Confirm it fails if the implementation were to close over `scansDir` at registration time, then confirm it passes against the actual 4.5 implementation.
- [ ] 4.7 Extend `tests/e2e/scan-preview.e2e.ts` with a case asserting a scan image still loads and displays correctly under the new `bloom-scan://` scheme, including a Windows-shaped path (built via `path.win32`, not the host platform's `path` module) in the scan fixture data used by the test, per Tier 5's Windows deployment target. **Update the existing "should load images from local filesystem" test**, which currently asserts `expect(src).toContain('file://')` — this will fail once `pathToFileUrl()` changes scheme and must be rewritten to assert `bloom-scan://` instead. **Also update `tests/e2e/scan-directory-format.e2e.ts`**, which has three more `expect(src).toContain('file://')`-style assertions against the same `pathToFileUrl()` call site — these were missed in an earlier pass and will also break once 4.4 lands. Note: this suite's `launchElectronApp()` only ever selects the dev-server (`localhost`) window, so the "Custom scheme accessible from packaged file origin" spec scenario has no real E2E coverage here — flag this explicitly as a known gap deferred to Tier 5's manual packaged-app QA pass, not silently treated as covered.
- [ ] 4.8 Run `npm run lint && npx tsc --noEmit && npm run test:unit`; run the E2E suite if runnable in this environment (see prior sessions' note that `npm run start` can behave differently in sandboxed environments — if it can't run locally, flag this explicitly as unverified-locally, to be confirmed on CI, rather than silently skipping it).

## 5. Type-Safe ImageUploader Fields (#97)

- [ ] 5.1 Replace the 4 `any`-typed fields in `src/main/image-uploader.ts:96-103` with `TypedSupabaseClient` (from `@salk-hpi/bloom-js`), `SupabaseUploader`, `SupabaseStore` (both `@salk-hpi/bloom-js` classes), and `typeof uploadImages` (imported from `@salk-hpi/bloom-fs`) — matching the types already assigned to these fields in `authenticate()` (lines 140-163). Remove the now-unnecessary `eslint-disable @typescript-eslint/no-explicit-any` comments.
- [ ] 5.2 Run `npx tsc --noEmit` to confirm the real types actually match every usage site in this file (this is the test signal for this task — a type-safety refactor with no behavior change has no new runtime test to write). Run `npm run lint && npm run test:unit` to confirm no regressions in existing `image-uploader` test coverage.

## 6. Verify (Not Fix) Scanner Thread Safety (#40)

- [ ] 6.1 Add a regression test to `python/tests/test_scanner.py` documenting the single-threaded-safety argument — at minimum, assert that `python/ipc_handler.py`'s only caller of `handle_scanner_command()` (and therefore `Scanner.perform_scan()`/`cleanup()`) is the synchronous `run_ipc_loop()` path, and that `_streaming_thread`'s target function (`streaming_worker()`) never references `Scanner`, `is_scanning`, or `_scanner_instance` (e.g. via a source-inspection assertion on `streaming_worker`'s code, or an explicit comment-documented invariant test). This test's docstring should explicitly state it exists in place of a `threading.Lock`, per the 2026-08-03 decision, and that if it starts failing, the lock decision should be re-evaluated, not just the test loosened.
- [ ] 6.2 Add a cheap runtime check at the top of `Scanner.perform_scan()` and `Scanner.cleanup()`: `if threading.current_thread() is not threading.main_thread(): raise RuntimeError("Scanner methods must be called from the main IPC thread")` — **an explicit `if`/`raise`, not a bare `assert`**, since Python strips `assert` under `-O`/`PYTHONOPTIMIZE` and this check must survive as a real safeguard in the packaged Windows build Tier 5 targets. Add a test confirming this check passes (no exception) under normal main-thread usage, and a test confirming it raises when called from a different thread (e.g. spin up a throwaway thread in the test and call `perform_scan()`/`cleanup()` from it, expecting `RuntimeError`). Add a note (comment or test docstring) that this exists specifically because of the #40 decision, not as unrelated defensive coding.
- [ ] 6.3 Add a regression test for the "Scan-in-progress cleanup is rejected immediately" `scanner-api` spec scenario — confirmed via `scanner.py:105-106` that `cleanup()` already raises `RuntimeError("Cannot cleanup during active scan")` when `is_scanning` is `True`, but no existing test exercises this. Add one asserting exactly this behavior (a scenario the new `scanner-api` capability names but that had zero test coverage anywhere in the codebase before this proposal).
- [ ] 6.4 Run `uv run pytest python/tests/test_scanner.py` to confirm the new tests and the new runtime check all pass against current code (they should pass immediately — this documents existing safety, it doesn't fix a bug).

## 7. Spec & Validation

- [ ] 7.1 Run `npx openspec validate harden-cylinderscan-tier1 --strict` and resolve any reported issues.
- [ ] 7.2 Run the full suite — `npm run lint`, `npx tsc --noEmit`, `npm run test:unit`, `uv run pytest python/`, and whatever E2E tests are runnable in this environment — confirming no regressions beyond this proposal's intended changes (per the roadmap's "No regressions" validation target). Explicitly confirm the following existing tests were actually updated (or, for `test_send_data`, confirmed to still pass unmodified), not just left to fail: `test_ipc_handler.py::test_send_error`, `test_ipc_handler.py::test_send_data` (should pass unmodified — the `id` key is omitted, not nulled, when unset), `python-process.test.ts`'s id-less `data` event test, `file-url.test.ts`'s ~8 `file://` assertions, `scan-preview.e2e.ts`'s `file://` assertion, and `scan-directory-format.e2e.ts`'s three `file://` assertions. Note any tests that could not be run locally (e.g. full Electron E2E) explicitly, to be confirmed once CI runs them, rather than silently treated as passing.
