## 1. Single Instance Application Lock (#249)

- [ ] 1.1 Write a failing unit test (new file `tests/unit/single-instance-lock.test.ts`) for a small extracted helper, e.g. `shouldQuitAsSecondInstance(hasLock: boolean): boolean` and a `focusExistingWindow(win: BrowserWindow | null)` helper — asserting: `shouldQuitAsSecondInstance(false)` returns `true` (lock not acquired → quit); `shouldQuitAsSecondInstance(true)` returns `false`; `focusExistingWindow` calls `restore()` when the window `isMinimized()`, and always calls `focus()`; **`focusExistingWindow(null)` does not throw and is a no-op** (covers the case where `second-instance` fires before `createWindow()` has run). Confirm it fails (helpers don't exist yet). Extracting these as small pure/near-pure functions makes the startup logic testable without spinning up a real second Electron process.
- [ ] 1.2 Implement the helpers in `src/main/main.ts` (or a small new `src/main/single-instance.ts` if that keeps `main.ts` cleaner) and call `app.requestSingleInstanceLock()` immediately after the existing `electron-squirrel-startup` check (`main.ts:69-74`), before any `app.on('ready', ...)` registration: if the lock isn't acquired, call `app.quit()` and return early. Register `app.on('second-instance', () => focusExistingWindow(mainWindow))` (passing the current value of `mainWindow`, which may still be `null`). Confirm 1.1 passes.
- [ ] 1.3 Run `npm run lint && npx tsc --noEmit && npm run test:unit`; fix any fallout.

## 2. Preload Listener Cleanup (#96)

> The real, reported leak lives in `PythonStatus.tsx`'s consumption of `python.onStatus`/`onError`, not just in `preload.ts` itself — this section fixes both the plumbing and the one real consumer, plus the `electron.d.ts` types both sides rely on.

- [ ] 2.1 Create `tests/unit/preload-listener-cleanup.test.ts` importing the real `src/main/preload.ts` module (following `tests/unit/preload-scanner.test.ts`'s mocking approach for `ipcRenderer`, but against the real module rather than a hand-mocked mirror). Write failing tests asserting each of the 8 target listeners returns a function, and that calling it results in `ipcRenderer.removeListener` being called with the correct channel name and the same listener reference passed to `ipcRenderer.on`: `pythonAPI.onStatus`, `pythonAPI.onError`, `cameraAPI.onTrigger`, `cameraAPI.onImageCaptured`, `daqAPI.onInitialized`, `daqAPI.onPositionChanged`, `daqAPI.onHome`, `daqAPI.onError`. Also assert **calling the returned cleanup function a second time does not throw** (idempotency), matching this repo's existing convention for double-cleanup safety (see `scanning/spec.md`'s "Double cleanup is safe" pattern for `ScannerSubprocess` worker management). Confirm all fail (no cleanup function currently returned).
- [ ] 2.2 Update the 8 listed methods in `src/main/preload.ts` to match the existing `cameraAPI.onFrame` (87-92) pattern: capture the listener in a named const, call `ipcRenderer.on(channel, listener)`, return `() => ipcRenderer.removeListener(channel, listener)` (`removeListener` is naturally idempotent — calling it twice with an already-removed listener is a no-op, not an error). Confirm 2.1 passes.
- [ ] 2.3 Update `src/types/electron.d.ts`: change the 8 target methods' return type on `PythonAPI`/`CameraAPI`/`DAQAPI` from `void` to `() => void`, matching `CameraAPI.onFrame`'s existing signature. Run `npx tsc --noEmit` — this alone won't fail (a wider return type is compatible with existing callers that ignore the return value), but it's required for `npx tsc --noEmit` to actually catch a future regression here, which was a gap in the original scope.
- [ ] 2.4 Add a failing test to `tests/unit/pages/Home.test.tsx` (or a dedicated `PythonStatus.test.tsx` if `PythonStatus` isn't already directly unit-tested — check during implementation): rendering then unmounting a component that uses `PythonStatus` calls the cleanup functions returned by `python.onStatus`/`python.onError` (assert via a mocked `onStatus`/`onError` that returns a jest/vitest mock cleanup fn, then assert that mock was called on unmount); a status/error event dispatched after unmount does not update any post-unmount state. Confirm it fails against current code (`PythonStatus.tsx`'s `useEffect` doesn't capture or return cleanup at all today).
- [ ] 2.5 Update `PythonStatus.tsx`'s `useEffect` (lines ~29-54) to capture the cleanup functions returned by `onStatus`/`onError` and return a combined cleanup (`return () => { cleanupStatus(); cleanupError(); }`) from the effect. Confirm 2.4 passes.
- [ ] 2.6 Run `npm run lint && npx tsc --noEmit && npm run test:unit`; fix any fallout.

## 3. IPC Command Response Correlation (#47)

> Revised commit-safety note (per review): the Python-side `id`-echo is additive and backward-compatible on its own (old TS code ignores an extra `id` key), so it's safe to land as its own smaller commit *before* the TS-side change — only the reverse order (TS expecting an id Python doesn't send) is unsafe. Sub-tasks below are ordered Python-first for exactly this reason, and can be split into two commits if convenient; they don't need to land in the same one.

- [ ] 3.1 Write failing Python tests in `python/tests/test_ipc_handler.py` (or create it if no such file exists — confirm during implementation; add a fixture that resets the module-level `_current_request_id` to `None` before/after each test to avoid cross-test state leakage): `send_data()` includes the module-level `_current_request_id` as an `"id"` key in its emitted JSON when one is set, and omits/nulls it when none is set; `send_error(message, tag_request=True)` (default) includes the current id in its emitted payload; **`send_error(message, tag_request=False)` never includes an id, regardless of what `_current_request_id` currently holds** (this is the fix for the streaming-thread bug found during review — see `design.md`). Decide the minimal wire-format change needed to carry an id on error responses (e.g. switching error responses to a `DATA:`-style JSON envelope with `success: false`, matching the existing `except Exception as e: send_data({"success": False, "error": str(e)})` pattern already used in `handle_scanner_command`'s catch-all, rather than a bespoke `ERROR:<id>:<message>` format). Confirm tests fail against current code. **Update the existing `test_send_error` test in this same file** (which currently asserts the exact literal output `"ERROR:test error\n"`) to match whatever wire format is chosen — this existing assertion will otherwise break.
- [ ] 3.2 Implement: `handle_command()` sets a module-level `_current_request_id = cmd.get("id")` at the top (before dispatching to `ping`/`get_version`/`check_hardware`/`camera`/`daq`/`scanner` branches), reset to `None` after the command completes (success or exception). `send_data()` reads `_current_request_id` unconditionally (grep-confirmed it's never called from `streaming_worker()` or any other thread). `send_error()` gains a `tag_request: bool = True` parameter; when `True`, it reads `_current_request_id`; when `False`, it omits the id entirely. Confirm 3.1 passes.
- [ ] 3.3 Update `streaming_worker()`'s two `send_error()` call sites (camera-unavailable check, catch-all exception handler) to pass `tag_request=False` explicitly — this is the actual bug fix, not just plumbing: without it, a streaming-thread error fired while an unrelated main-thread command is in flight would incorrectly reject that unrelated command. Add a Python test asserting this: simulate `_current_request_id` being set to some value (as if a command were in flight), call `send_error(msg, tag_request=False)` (as `streaming_worker()` does), and assert the emitted payload carries no id despite `_current_request_id` being non-`None`.
- [ ] 3.4 Remove the hardcoded ~1-second pre-camera-detection delay landed in PR #41 as a workaround for this same race (locate via `git log`/`git blame` or a search for the delay's comment referencing this issue) — it's dead weight once real correlation exists, and leaving it would mislead a future reader into thinking it's still load-bearing.
- [ ] 3.5 Write failing TypeScript tests in `tests/unit/python-process.test.ts` (extending the existing file): (a) two overlapping `sendCommand()` calls each resolve with the response matching their own request id, even if responses arrive out of order; (b) an error response carrying a specific id rejects only that pending call, leaving a second concurrent call still pending; (c) an error event with no id rejects every currently-pending call; (d) a response whose id has already timed out and been removed from `pendingRequests` is ignored (no resolve/reject, no error thrown) rather than causing a crash. **Update the existing test that emits `process.emit('data', { success: true })` with no `id`** — under the new design this payload has no matching pending-request entry and must not resolve any promise; rewrite that test's expectation (or give it a matching id) rather than leaving it to hang. Confirm all new/updated tests fail against current code (single global `once` pair can't support this).
- [ ] 3.6 Implement in `src/main/python-process.ts`: add a private incrementing counter and `pendingRequests: Map<number, {resolve, reject, timeoutId}>`; rewrite `sendCommand()` to generate the next id, store its handlers in the map, send `{...command, id}`; replace the per-call `once('data'/'error', ...)` with persistent listeners (registered once, e.g. in the constructor) that look up the incoming payload's `id` in `pendingRequests` and resolve/reject/delete only that entry, or (for an id-less error) reject and clear every entry in the map; ensure a timed-out request's map entry is deleted when its timeout fires. Confirm 3.5 passes.
- [ ] 3.7 Add a test (TypeScript or Python, whichever is more natural given where `ScanResult`/metadata assembly actually happens — check `scanner-process.ts`'s `saveScanToDatabase()` during implementation) asserting the new `id` field never appears in a persisted `Scan`/`Image` record or `metadata.json` — defensive coverage for the "no effect on scan data" claim in `design.md`, rather than relying on "nobody spreads the response object today" as an implicit invariant.
- [ ] 3.8 Run `npm run lint && npx tsc --noEmit && npm run test:unit && uv run pytest python/tests/test_ipc_handler.py python/tests/test_scanner.py`; fix any fallout.

## 4. Custom Protocol Scheme Replacing `webSecurity: false` (#93)

- [ ] 4.1 Write failing unit tests for a new path-validation helper (e.g. `resolveScanPath(requestedPath: string, scansDir: string): string | null` in a new `src/main/scan-protocol.ts`, returning the resolved absolute path if it's contained within `scansDir`, `null` otherwise): a path inside `scansDir` resolves correctly; a path containing `..` segments that would escape `scansDir` returns `null`; an absolute-path-override attempt returns `null` — **use `path.win32.resolve()` explicitly (not the platform-dependent `path` module) for the Windows-drive-letter-override case**, so this test is deterministic on Linux/macOS CI runners too, not just when run on Windows; **a same-directory path differing only in case is accepted on Windows** (case-insensitive containment check) — parameterize this sub-case behind a `process.platform === 'win32'` branch or an explicit `platform` parameter so it's testable without actually running on Windows. Confirm tests fail (helper doesn't exist).
- [ ] 4.2 Implement `resolveScanPath()` using `path.resolve()` + a starts-with containment check against the resolved `scansDir`, lower-casing both sides of the comparison when running on (or targeting) Windows. Confirm 4.1 passes.
- [ ] 4.3 Write failing unit tests **rewriting** `src/utils/file-url.ts`'s existing test coverage (find or create `tests/unit/file-url.test.ts` — if it exists, its current ~8 cases all assert `file://` output and must be rewritten, not left alongside new ones): `pathToFileUrl()` now emits `bloom-scan://` instead of `file://`, preserving the existing backslash-normalization, drive-letter, and percent-encoding behavior (e.g. `C:\Users\foo\bar.png` → `bloom-scan:///C:/Users/foo/bar.png`). Confirm it fails against current `file://`-emitting code.
- [ ] 4.4 Update `pathToFileUrl()` in `src/utils/file-url.ts` to emit the `bloom-scan://` scheme. Confirm 4.3 passes.
- [ ] 4.5 In `src/main/main.ts`: call `protocol.registerSchemesAsPrivileged([{ scheme: 'bloom-scan', privileges: { standard: true, secure: true, supportFetchAPI: true, corsEnabled: true } }])` at module load time (before `app.on('ready', ...)`); inside the existing `'ready'` handler, register `protocol.handle('bloom-scan', (request) => { ... })` **before** the existing `createWindow()`/`loadURL()` call (not after), reading the currently-configured `scans_dir` fresh from the config-store accessor on every request (not a value captured once at registration time) and calling `resolveScanPath()` to serve the file or reject anything outside it. Remove `webSecurity: false` from the `BrowserWindow` `webPreferences`. This has no dedicated new unit test beyond 4.1/4.2 (protocol registration itself is best verified by the E2E test in 4.7) but confirm `npx tsc --noEmit` passes and no other test regresses.
- [ ] 4.6 Update `ScanPreview.tsx`'s existing image `onError` handling (per the accepted "Scan Preview Image Error Handling" requirement) to distinguish a protocol-rejected load (e.g. a distinguishable error code/response the `bloom-scan://` handler returns for a rejected request) from a generic missing-file failure, and surface a correspondingly distinct message. Add/update a unit test for this distinction.
- [ ] 4.7 Extend `tests/e2e/scan-preview.e2e.ts` with a case asserting a scan image still loads and displays correctly under the new `bloom-scan://` scheme, including a Windows-shaped path (built via `path.win32`, not the host platform's `path` module) in the scan fixture data used by the test, per Tier 5's Windows deployment target. **Update the existing "should load images from local filesystem" test**, which currently asserts `expect(src).toContain('file://')` — this will fail once `pathToFileUrl()` changes scheme and must be rewritten to assert `bloom-scan://` instead. Note: this suite's `launchElectronApp()` only ever selects the dev-server (`localhost`) window, so the "Custom scheme accessible from packaged file origin" spec scenario has no real E2E coverage here — flag this explicitly as a known gap deferred to Tier 5's manual packaged-app QA pass, not silently treated as covered.
- [ ] 4.8 Run `npm run lint && npx tsc --noEmit && npm run test:unit`; run the E2E suite if runnable in this environment (see prior sessions' note that `npm run start` can behave differently in sandboxed environments — if it can't run locally, flag this explicitly as unverified-locally, to be confirmed on CI, rather than silently skipping it).

## 5. Type-Safe ImageUploader Fields (#97)

- [ ] 5.1 Replace the 4 `any`-typed fields in `src/main/image-uploader.ts:96-103` with `TypedSupabaseClient` (from `@salk-hpi/bloom-js`), `SupabaseUploader`, `SupabaseStore` (both `@salk-hpi/bloom-js` classes), and `typeof uploadImages` (imported from `@salk-hpi/bloom-fs`) — matching the types already assigned to these fields in `authenticate()` (lines 140-163). Remove the now-unnecessary `eslint-disable @typescript-eslint/no-explicit-any` comments.
- [ ] 5.2 Run `npx tsc --noEmit` to confirm the real types actually match every usage site in this file (this is the test signal for this task — a type-safety refactor with no behavior change has no new runtime test to write). Run `npm run lint && npm run test:unit` to confirm no regressions in existing `image-uploader` test coverage.

## 6. Mode-Aware Python Backend Status (#198)

> Decision (per `design.md`): `PythonStatus` renders `null` entirely in `graviscan` mode (not just its hardware rows hidden) — there's no GraviScan-relevant content this tier adds to show instead.

- [ ] 6.1 Add a failing case to `tests/unit/pages/Home.test.tsx` (which already mocks `window.electron.python` and tests mode branching for other elements): with `mode="graviscan"`, no "Python Backend Status" heading or Camera/DAQ content renders anywhere; with `mode="cylinderscan"`, the heading and (after triggering `checkHardware()`, e.g. via the existing button/interaction the component uses to populate hardware state — check `PythonStatus.tsx`'s current implementation for how `hardware` state gets populated, since it's `null` until that happens) hardware content is present. Confirm it fails against current code (unconditional rendering).
- [ ] 6.2 Add a `mode` prop to `PythonStatus`'s component signature (`src/renderer/components/PythonStatus.tsx`) and return `null` immediately when `mode !== 'cylinderscan'`, before rendering anything (heading included). Update `Home.tsx:60-63` to pass `<PythonStatus mode={mode} />` using its already-available `mode` prop. Confirm 6.1 passes.
- [ ] 6.3 Run `npm run lint && npx tsc --noEmit && npm run test:unit`; fix any fallout.

## 7. Verify (Not Fix) Scanner Thread Safety (#40)

- [ ] 7.1 Add a regression test to `python/tests/test_scanner.py` documenting the single-threaded-safety argument — at minimum, assert that `python/ipc_handler.py`'s only caller of `handle_scanner_command()` (and therefore `Scanner.perform_scan()`/`cleanup()`) is the synchronous `run_ipc_loop()` path, and that `_streaming_thread`'s target function (`streaming_worker()`) never references `Scanner`, `is_scanning`, or `_scanner_instance` (e.g. via a source-inspection assertion on `streaming_worker`'s code, or an explicit comment-documented invariant test — decide the concrete mechanism during implementation, since this is closer to an architectural-invariant test than a behavioral one). This test's docstring should explicitly state it exists in place of a `threading.Lock`, per the 2026-08-03 decision, and that if it starts failing, the lock decision should be re-evaluated, not just the test loosened.
- [ ] 7.2 Add a cheap runtime assertion — `assert threading.current_thread() is threading.main_thread()` — at the top of `Scanner.perform_scan()` and `Scanner.cleanup()`, as a belt-and-suspenders tripwire alongside the test (per review: a test alone can bit-rot silently; this fails loudly in production the instant the single-threaded invariant is ever broken). Add a test confirming this assertion passes under normal (main-thread) usage and add a note (comment or test docstring) that this exists specifically because of the #40 decision, not as unrelated defensive coding.
- [ ] 7.3 Run `uv run pytest python/tests/test_scanner.py` to confirm both the new test and the new assertion pass against current code (they should pass immediately — this documents existing safety, it doesn't fix a bug).

## 8. Spec & Validation

- [ ] 8.1 Run `npx openspec validate harden-cylinderscan-tier1 --strict` and resolve any reported issues.
- [ ] 8.2 Run the full suite — `npm run lint`, `npx tsc --noEmit`, `npm run test:unit`, `uv run pytest python/`, and whatever E2E tests are runnable in this environment — confirming no regressions beyond this proposal's intended changes (per the roadmap's "No regressions" validation target). Explicitly confirm the four existing tests flagged above as needing updates (`test_ipc_handler.py::test_send_error`, `python-process.test.ts`'s id-less `data` event test, `file-url.test.ts`'s ~8 `file://` assertions, `scan-preview.e2e.ts`'s `file://` assertion) were actually updated, not just left to fail. Note any tests that could not be run locally (e.g. full Electron E2E) explicitly, to be confirmed once CI runs them, rather than silently treated as passing.
