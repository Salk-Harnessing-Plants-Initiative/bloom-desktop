## 1. Single Instance Application Lock (#249)

- [ ] 1.1 Write a failing unit test (new file `tests/unit/single-instance-lock.test.ts`) for a small extracted helper, e.g. `shouldQuitAsSecondInstance(hasLock: boolean): boolean` and a `focusExistingWindow(win: BrowserWindow)` helper — asserting: `shouldQuitAsSecondInstance(false)` returns `true` (lock not acquired → quit); `shouldQuitAsSecondInstance(true)` returns `false`; `focusExistingWindow` calls `restore()` when the window `isMinimized()`, and always calls `focus()`. Confirm it fails (helpers don't exist yet). Extracting these as small pure/near-pure functions makes the startup logic testable without spinning up a real second Electron process.
- [ ] 1.2 Implement the helpers in `src/main/main.ts` (or a small new `src/main/single-instance.ts` if that keeps `main.ts` cleaner) and call `app.requestSingleInstanceLock()` immediately after the existing `electron-squirrel-startup` check (`main.ts:69-74`), before any `app.on('ready', ...)` registration: if the lock isn't acquired, call `app.quit()` and return early. Register `app.on('second-instance', () => focusExistingWindow(mainWindow))`. Confirm 1.1 passes.
- [ ] 1.3 Run `npm run lint && npx tsc --noEmit && npm run test:unit`; fix any fallout.

## 2. Preload Listener Cleanup (#96)

- [ ] 2.1 Create `tests/unit/preload-listener-cleanup.test.ts` importing the real `src/main/preload.ts` module (following `tests/unit/preload-scanner.test.ts`'s mocking approach for `ipcRenderer`, but against the real module rather than a hand-mocked mirror). Write failing tests asserting each of the 8 target listeners returns a function, and that calling it results in `ipcRenderer.removeListener` being called with the correct channel name and the same listener reference passed to `ipcRenderer.on`: `pythonAPI.onStatus`, `pythonAPI.onError`, `cameraAPI.onTrigger`, `cameraAPI.onImageCaptured`, `daqAPI.onInitialized`, `daqAPI.onPositionChanged`, `daqAPI.onHome`, `daqAPI.onError`. Confirm all fail (no cleanup function currently returned).
- [ ] 2.2 Update the 8 listed methods in `src/main/preload.ts` to match the existing `cameraAPI.onFrame` (87-92) pattern: capture the listener in a named const, call `ipcRenderer.on(channel, listener)`, return `() => ipcRenderer.removeListener(channel, listener)`. Confirm 2.1 passes.
- [ ] 2.3 Run `npm run lint && npx tsc --noEmit && npm run test:unit`; fix any fallout.

## 3. IPC Command Response Correlation (#47)

> Python-side and TypeScript-side halves are both needed for this to work end-to-end; land them in the same commit per the "commit-safety" convention used elsewhere in this repo's OpenSpec history, since a request id sent by TS with no Python-side echo (or vice versa) leaves the feature half-working.

- [ ] 3.1 Write failing Python tests in `python/tests/test_ipc_handler.py` (or create it if no such file exists — confirm during implementation): `send_data()` includes the module-level `_current_request_id` as an `"id"` key in its emitted JSON when one is set, and omits/nulls it when none is set; same for `send_error()`'s emitted payload structure (check how errors are currently serialized — via `ERROR:` prefix, a plain string — and decide the minimal structural change needed to carry an id, e.g. `ERROR:<id>:<message>` or switching error responses to a `DATA:`-style JSON envelope with a `success: false` field, matching the existing `except Exception as e: send_data({"success": False, "error": str(e)})` pattern already used in `handle_scanner_command`'s catch-all). Confirm tests fail against current code.
- [ ] 3.2 Implement: `handle_command()` sets a module-level `_current_request_id = cmd.get("id")` at the top (before dispatching to `ping`/`get_version`/`check_hardware`/`camera`/`daq`/`scanner` branches), reset to `None` after the command completes (success or exception). `send_data()` and `send_error()` read `_current_request_id` and include it in their emitted payload. Confirm 3.1 passes.
- [ ] 3.3 Write failing TypeScript tests in `tests/unit/python-process.test.ts` (extending the existing file): (a) two overlapping `sendCommand()` calls each resolve with the response matching their own request id, even if responses arrive out of order; (b) an `ERROR:`/data-with-`success:false` response carrying a specific id rejects only that pending call, leaving a second concurrent call still pending; (c) an error event with no id rejects every currently-pending call. Confirm all fail against current code (single global `once` pair can't support this).
- [ ] 3.4 Implement in `src/main/python-process.ts`: add a private incrementing counter and `pendingRequests: Map<number, {resolve, reject, timeoutId}>`; rewrite `sendCommand()` to generate the next id, store its handlers in the map, send `{...command, id}`; replace the per-call `once('data'/'error', ...)` with persistent listeners (registered once, e.g. in the constructor) that look up the incoming payload's `id` in `pendingRequests` and resolve/reject/delete only that entry, or (for an id-less error) reject and clear every entry in the map. Confirm 3.3 passes.
- [ ] 3.5 Run `npm run lint && npx tsc --noEmit && npm run test:unit && uv run pytest python/tests/test_ipc_handler.py python/tests/test_scanner.py`; fix any fallout.

## 4. Custom Protocol Scheme Replacing `webSecurity: false` (#93)

- [ ] 4.1 Write failing unit tests for a new path-validation helper (e.g. `resolveScanPath(requestedPath: string, scansDir: string): string | null` in a new `src/main/scan-protocol.ts`, returning the resolved absolute path if it's contained within `scansDir`, `null` otherwise): a path inside `scansDir` resolves correctly; a path containing `..` segments that would escape `scansDir` returns `null`; an absolute path override attempt (e.g. a second drive letter on Windows) returns `null`. Confirm tests fail (helper doesn't exist).
- [ ] 4.2 Implement `resolveScanPath()` using `path.resolve()` + a starts-with check against the resolved `scansDir`. Confirm 4.1 passes.
- [ ] 4.3 Write failing unit tests updating `src/utils/file-url.ts`'s existing test coverage (find or create `tests/unit/file-url.test.ts`): `pathToFileUrl()` now emits `bloom-scan://` instead of `file://`, preserving the existing backslash-normalization, drive-letter, and percent-encoding behavior (e.g. `C:\Users\foo\bar.png` → `bloom-scan:///C:/Users/foo/bar.png`). Confirm it fails against current `file://`-emitting code.
- [ ] 4.4 Update `pathToFileUrl()` in `src/utils/file-url.ts` to emit the `bloom-scan://` scheme. Confirm 4.3 passes.
- [ ] 4.5 In `src/main/main.ts`: call `protocol.registerSchemesAsPrivileged([{ scheme: 'bloom-scan', privileges: { standard: true, secure: true, supportFetchAPI: true, corsEnabled: true } }])` at module load time (before `app.on('ready', ...)`); inside the existing `'ready'` handler, register `protocol.handle('bloom-scan', (request) => { ... })` using `resolveScanPath()` against the currently-configured `scans_dir` (read via the existing config-store accessor) to serve the file or return a rejection for anything outside it. Remove `webSecurity: false` from the `BrowserWindow` `webPreferences`. This has no dedicated new unit test beyond 4.1/4.2 (protocol registration itself is best verified by the E2E test in 4.6) but confirm `npx tsc --noEmit` passes and no other test regresses.
- [ ] 4.6 Extend `tests/e2e/scan-preview.e2e.ts` with a case asserting a scan image still loads and displays correctly under the new `bloom-scan://` scheme, including a Windows-shaped path (drive letter + backslashes) in the scan fixture data used by the test, per Tier 5's Windows deployment target.
- [ ] 4.7 Run `npm run lint && npx tsc --noEmit && npm run test:unit`; run the E2E suite if runnable in this environment (see prior sessions' note that `npm run start` can behave differently in sandboxed environments — if it can't run locally, flag this explicitly as unverified-locally, to be confirmed on CI, rather than silently skipping it).

## 5. Type-Safe ImageUploader Fields (#97)

- [ ] 5.1 Replace the 4 `any`-typed fields in `src/main/image-uploader.ts:96-103` with `TypedSupabaseClient` (from `@salk-hpi/bloom-js`), `SupabaseUploader`, `SupabaseStore` (both `@salk-hpi/bloom-js` classes), and `typeof uploadImages` (imported from `@salk-hpi/bloom-fs`) — matching the types already assigned to these fields in `authenticate()` (lines 140-163). Remove the now-unnecessary `eslint-disable @typescript-eslint/no-explicit-any` comments.
- [ ] 5.2 Run `npx tsc --noEmit` to confirm the real types actually match every usage site in this file (this is the test signal for this task — a type-safety refactor with no behavior change has no new runtime test to write). Run `npm run lint && npm run test:unit` to confirm no regressions in existing `image-uploader` test coverage.

## 6. Mode-Aware Python Backend Status (#198)

- [ ] 6.1 Add a failing case to `tests/unit/pages/Home.test.tsx` (which already mocks `window.electron.python` and tests mode branching for other elements): with `mode="graviscan"`, "Python Backend Status" / Camera / DAQ content is absent; with `mode="cylinderscan"`, it's present. Confirm it fails against current code (unconditional rendering).
- [ ] 6.2 Add a `mode` prop to `PythonStatus`'s component signature (`src/renderer/components/PythonStatus.tsx`) and gate the Camera/DAQ-specific rendering (lines 110-151) on `mode === 'cylinderscan'`. Update `Home.tsx:60-63` to pass `<PythonStatus mode={mode} />` using its already-available `mode` prop. Confirm 6.1 passes.
- [ ] 6.3 Run `npm run lint && npx tsc --noEmit && npm run test:unit`; fix any fallout.

## 7. Verify (Not Fix) Scanner Thread Safety (#40)

- [ ] 7.1 Add a regression test to `python/tests/test_scanner.py` documenting the single-threaded-safety argument — at minimum, assert that `python/ipc_handler.py`'s only caller of `handle_scanner_command()` (and therefore `Scanner.perform_scan()`/`cleanup()`) is the synchronous `run_ipc_loop()` path, and that `_streaming_thread`'s target function (`streaming_worker()`) never references `Scanner`, `is_scanning`, or `_scanner_instance` (e.g. via a source-inspection assertion on `streaming_worker`'s code, or an explicit comment-documented invariant test — decide the concrete mechanism during implementation, since this is closer to an architectural-invariant test than a behavioral one). This test's docstring should explicitly state it exists in place of a `threading.Lock`, per the 2026-08-03 decision, and that if it starts failing, the lock decision should be re-evaluated, not just the test loosened.
- [ ] 7.2 Run `uv run pytest python/tests/test_scanner.py` to confirm the new test passes against current code (it should pass immediately — this documents existing safety, it doesn't fix a bug).

## 8. Spec & Validation

- [ ] 8.1 Run `npx openspec validate cylinderscan-tier1-hardening --strict` and resolve any reported issues.
- [ ] 8.2 Run the full suite — `npm run lint`, `npx tsc --noEmit`, `npm run test:unit`, `uv run pytest python/`, and whatever E2E tests are runnable in this environment — confirming no regressions beyond this proposal's intended changes (per the roadmap's "No regressions" validation target). Note any tests that could not be run locally (e.g. full Electron E2E) explicitly, to be confirmed once CI runs them, rather than silently treated as passing.
