## 1. Fix the Linux executable-naming bug (design.md Decision 2)

- [x] 1.1 Write the test first: `tests/unit/resolve-executable-name.test.ts`, asserting `resolveExecutableName('linux')` returns `'bloom-desktop'`, and `resolveExecutableName('darwin')`/`resolveExecutableName('win32')` both return `'Bloom Desktop'` (the existing, already-correct value). Must fail before 1.2's implementation exists (the module doesn't exist yet).
- [x] 1.2 Implement `scripts/resolve-executable-name.ts` exporting `resolveExecutableName(platform: NodeJS.Platform): string`, a pure function (no `process.platform` read internally — takes it as an argument, matching `resolveStagedAppPath`'s existing pattern so it stays unit-testable without mocking module-load-time globals). Make 1.1's test pass.
- [x] 1.3 Wire it into `forge.config.ts`: import `resolveExecutableName` and set `packagerConfig.executableName: resolveExecutableName(process.platform)`.
- [x] 1.4 Confirm against real hardware: this was already empirically confirmed during proposal scoping (see design.md Decision 2) — after this fix, `npx electron-forge package` on a real Linux machine (`pbiob-gh-04`) stages the binary at `out/Bloom Desktop-linux-x64/bloom-desktop` (matching `resolveStagedAppPath`'s existing, unmodified `linux` case), and macOS/Windows staging is unaffected (the ternary's non-`linux` branch returns the same `'Bloom Desktop'` value electron-packager already defaults to). Re-confirm once this task's actual code change lands (not just the scratch-patched version used during scoping) by re-running the same dry-run before pushing to CI, since implementation code can differ from what was hand-patched during scoping.

## 2. Extend maker-artifact verification for Linux (design.md Decision 1)

- [x] 2.1 Write the tests first, in `tests/unit/test-make-artifacts.test.ts`: (a) "finds a .deb under a Linux-shaped nested path" — mirrors the existing darwin/win32 positive-case tests; (b) "prefers .deb over .rpm when both exist on Linux" — a temp dir containing both a `.deb` and a `.rpm` file, asserting `findMakerArtifact(dir, 'linux')` returns the `.deb` path, locking in the decision that `.rpm` alone is not sufficient for the Linux case. Note: a test that only seeds a bare `.rpm` file and asserts `null` would be tautological — `findMakerArtifact` already returns `null` unconditionally for `'linux'` today (no `linux` key exists in `PLATFORM_EXTENSIONS` yet) and would keep returning `null` after 2.2 too, since `.rpm` is never added to that list; it would never meaningfully exercise post-2.2 behavior. The both-files variant in (b) is the one that actually goes red before 2.2 and green after. (a) must also fail before 2.2's implementation exists.
- [x] 2.2 Add `linux: ['.deb']` to `tests/integration/test-make-artifacts.ts`'s `PLATFORM_EXTENSIONS` map. Make 2.1's tests pass.
- [x] 2.3 Confirm against real hardware: already empirically confirmed during proposal scoping — after 2.2's change (hand-patched) and a real `.deb` produced by a scoped `npx electron-forge make --targets=deb` run on `pbiob-gh-04`, `npm run test:make:artifacts` printed `[PASS] Found maker artifact: .../bloom-desktop_0.1.0_amd64.deb (108551282 bytes)`. Re-confirm once this task's actual code change lands.

## 3. Scope Linux CI's maker stage to `.deb` only (design.md Decision 3)

- [x] 3.1 Add a `"make:linux": "npm run build:python && npm run build:native && electron-forge make --targets=deb"` script to `package.json`, alongside the existing `make` script. Use `--targets=deb` (the maker's short `name` property, which `electron-forge`'s target-resolution matches against the actually-configured `MakerDeb` instance in `forge.config.ts`) — **not** `--targets=@electron-forge/maker-deb` (the npm package name), which does not match `MakerDeb.name === 'deb'` and silently falls through to re-requiring the package fresh with none of `forge.config.ts`'s configuration (harmless only by coincidence today, since that config happens to be empty — found and corrected during this proposal's own review, see design.md Decision 3's flag-value note).
- [x] 3.2 Confirm against real hardware: already empirically confirmed during proposal scoping — with `--targets=deb` (and task 1's `executableName` fix applied), the make step completed cleanly and produced `out/make/deb/x64/bloom-desktop_0.1.0_amd64.deb`, correctly matching the configured `MakerDeb` instance (re-confirmed after correcting the flag value from the npm-package-name form, per 3.1's note); without the `--targets` scoping at all, the same run failed entirely (including the `.deb` output) due to `@electron-forge/maker-rpm` throwing on this environment's `rpm`/rpmdb permissions (see design.md Decision 3 for the full error). Re-confirm `npm run make:linux` (the actual script from 3.1, not the hand-run equivalent) produces a valid `.deb` before pushing to CI.

## 4. CI workflow changes

- [x] 4.1 Extend `tests/unit/pr-checks-workflow.test.ts`'s `WorkflowJob` interface first, since the current interface is a closed type with no `needs` or `runs-on` field (only `'timeout-minutes'`, `strategy`, and `steps` — `steps`' entries have a `[key: string]: unknown` index signature, but `WorkflowJob` itself does not): add `needs?: string | string[];` and `'runs-on'?: string;` to `WorkflowJob`. Without this, the assertions in 4.2 below will not compile under `tsc --noEmit` (task 6.1) — "Property 'needs' does not exist on type 'WorkflowJob'" — found during this proposal's own review, not something to discover mid-implementation.
- [x] 4.2 Write the tests first, in `tests/unit/pr-checks-workflow.test.ts`: (a) add `'test-make-linux': 20` to the existing `EXPECTED_TIMEOUTS` map — this `it.each` case fails with "job undefined" until 4.3 adds the real job, and the existing "does not set timeout-minutes on any job outside the exposed set" test would start failing the moment 4.3 lands without this update, so this must be committed first, not after, matching Sections 1/2's test-first ordering; (b) add new structural assertions on `workflow.jobs['test-make-linux']` (mirroring the existing `test-e2e-dev` sharding assertions' pattern of inspecting parsed job/step objects, using 4.1's extended interface): asserts `needs` includes `'build-python'` and `'runs-on'` is `'ubuntu-latest'`; asserts at least one step's `run` contains `npm run make:linux` (not bare `npm run make`) — a regression test against someone later "simplifying" the job back to the unscoped script and silently reintroducing the `maker-rpm` crash from design.md Decision 3; asserts the launch-verification step's `env` sets `ELECTRON_DISABLE_SANDBOX: 1` and its `run` is wrapped in `xvfb-run`. All of (a) and (b) must fail before 4.3 exists (the job doesn't exist yet, so `workflow.jobs['test-make-linux']` is `undefined`).
- [x] 4.3 Add the `test-make-linux` job to `.github/workflows/pr-checks.yml`: `needs: build-python`, `runs-on: ubuntu-latest`, `timeout-minutes: 20`. Steps: checkout, setup-node, cache node_modules, setup-uv, download `python-executable-Linux` artifact, `chmod +x dist/bloom-hardware`, install system packages (`sudo apt-get update && sudo apt-get install -y build-essential libusb-1.0-0-dev pkg-config fakeroot`), `npm ci`, `npx playwright install --with-deps`, `npx prisma generate` (`BLOOM_DATABASE_URL: 'file:./dev.db'`), `npm run make:linux` (the maker stage), `npm run test:make:artifacts`, then `xvfb-run --auto-servernum npm run test:package:launch` with `ELECTRON_DISABLE_SANDBOX: 1` set in that step's `env`. Make 4.2's tests pass.
- [x] 4.4 Add an `actions/upload-artifact` step after `npm run test:make:artifacts` passes, uploading `out/make/**` as `packaged-app-${{ runner.os }}` with `retention-days: 7` — identical pattern to `test-make`/`test-make-windows`.
- [x] 4.5 Add `test-make-linux` to `all-checks-passed`'s `needs` list.
- [x] 4.6 Confirm `git diff --unified=0` for `.github/workflows/pr-checks.yml` touches only additive lines around the new job and `all-checks-passed`'s `needs` list — the existing `test-make`/`test-make-windows`/every other job's own lines show zero diff.
- [ ] 4.7 There is no automated way to unit-test a GitHub Actions workflow file's actual execution in isolation, and this repo has no local Linux CI runner. The real CI run on this change's PR is the test for this section — push and confirm `Test - Make (Linux)` passes on the actual `ubuntu-latest` GitHub-hosted runner (not just `pbiob-gh-04`, which is a different Ubuntu version than GitHub's runner image — see design.md's Risks section). If system packages, `xvfb-run`, or the sandbox flag behave differently on the real runner than they did during scoping, adjust the job's steps and re-push rather than weakening any assertion to work around it.
- [ ] 4.8 Once 4.7's real CI run completes, record its actual observed duration and compare against the provisional `timeout-minutes: 20` from 4.3 — if the real duration is close to or exceeds 20 minutes, increase the bound (and update 4.2(a)'s `EXPECTED_TIMEOUTS` entry and design.md's Decision 1 note) rather than leaving a bound that risks false-terminating a normal run. Regardless of outcome, also update `specs/developer-workflows/spec.md`'s "Timeout values leave headroom for normal runs" scenario to record the real observed duration instead of "no observed CI duration yet" once this data exists, so the archived spec doesn't permanently assert a now-false claim.

## 5. Documentation (#295)

- [x] 5.1 Create `docs/GRAVISCAN_LINUX_DEPLOYMENT.md` covering: (a) the `libusb-filter.so`/`LD_PRELOAD` packaged-app mechanism — trace `forge.config.ts`'s Linux-only `extraResource` (bundles the `.so` into `Resources/libusb-filter.so`), `scanner-subprocess.ts`'s `spawn()` resolving `libusbFilterSoPath` via `process.resourcesPath` when packaged vs. `src/main/native/libusb-filter.so` in dev mode, and `buildSubprocessEnv()` setting `LD_PRELOAD`/`SANE_USB_FILTER`/`LIBUSB_ENDPOINT_RECOVERY` only on Linux + real (non-mock) mode per scan-worker subprocess spawn; state the concrete failure mode without the filter (epkowa claims USB interfaces on every connected Epson scanner, breaking parallel scanning — per the archived `add-ld-preload-to-subprocess` proposal); (b) the deb-vs-rpm-vs-AppImage choice — cite `graviscan-ms-7c56`/`docs/GRAVISCAN_SCANNER_DRIVER_SETUP.md` as evidence the real lab machine is apt-based, state `.rpm` has no current real-machine consumer, link to this change's design.md Context (AppImage decision) and Decision 3 (why CI only builds `.deb`); (c) systemd/permissions caveats for the packaged app — confirm whether any systemd unit/autostart exists in this repo (grep for `.service` files and `systemctl`/`systemd` references) and state plainly if none does rather than describing a hypothetical one; point to `docs/GRAVISCAN_SCANNER_DRIVER_SETUP.md`'s existing udev-rules coverage for the scanner's own USB permissions rather than duplicating it; note how `~/.bloom/data/bloom.db`'s per-user home-directory path (per `docs/PACKAGING.md`'s "Development vs Production" table) depends on which user account runs the packaged app.
- [x] 5.2 Add a link to `docs/GRAVISCAN_LINUX_DEPLOYMENT.md` in `docs/PACKAGING.md`'s "Related Documentation" section.
- [x] 5.3 Add a link to `docs/GRAVISCAN_LINUX_DEPLOYMENT.md` in `docs/GRAVISCAN_SCANNER_DRIVER_SETUP.md`'s "Related Documentation" section.
- [x] 5.4 Verification for this section (docs have no automated test suite of their own, but these are scriptable, concrete checks): `grep -rn "GRAVISCAN_LINUX_DEPLOYMENT" docs/PACKAGING.md docs/GRAVISCAN_SCANNER_DRIVER_SETUP.md` returns at least one hit in each file; `grep -c "LD_PRELOAD" docs/GRAVISCAN_LINUX_DEPLOYMENT.md` returns at least 1; `grep -c "systemd" docs/GRAVISCAN_LINUX_DEPLOYMENT.md` returns at least 1 (covering the "no unit exists today" statement if that's what's true).

## 6. Validation

- [x] 6.1 Run `npm run lint`, `npx tsc --noEmit`, and `npm run test:unit` (existing unit suite plus the new tests from 1.1, 2.1, 4.1/4.2) — all green.
- [x] 6.2 Run `openspec validate add-graviscan-linux-packaging-ci --strict` and fix any issues.
- [ ] 6.3 Push the branch and confirm `Test - Make (Linux)` passes on the real PR (per task 4.7), and confirm the existing `test-make`/`test-make-windows`/every other job's own runs are unaffected (per task 4.6).
