## 1. Main process — export handler

- [x] 1.1 Write failing test (`tests/unit/scans-export.test.ts`): happy path — exporting scans with no conflicts returns `{ success: true, data: { exportedFiles, exportedScans, skippedFiles: 0, failedScans: [] } }` and every file lands at `<destinationDir>/<scan.path>/<filename>`
- [x] 1.2 Write failing test: skip-on-conflict — a file already present at the destination is left untouched (content unchanged) and counted in `skippedFiles`, not `exportedFiles`; re-running an export against a destination where a scan's files are _already fully present_ from a prior run succeeds (not an error) and counts everything as skipped — this is the specific idempotent-resume case a whole-directory-rename design would break
- [x] 1.3 Write failing test: path containment — a scan whose `path` (mocked to contain a `..` segment or an absolute path) would resolve outside the configured `scans_dir` (source) or the destination directory is recorded in `failedScans` with a descriptive reason (including `scanId`, `experimentName`, `captureDate`), no file for that scan is written anywhere, and the batch continues to the next scan
- [x] 1.4a Write failing test: file-level atomicity — simulate a crash mid-write of a single file and assert that file never appears at its final filename in a truncated state (either absent or fully present), and that a stray `.tmp` file left behind is removed on the _next_ attempt at that file regardless of whether that next attempt ends up copying it or skipping it because it was later created some other way
- [x] 1.4b Write failing test: metadata-first ordering — for a scan whose source folder contains both `metadata.json` and `NNN.png` frames, assert `metadata.json` is copied even when the frame files fail (via forced per-file failures), proving metadata's copy isn't gated behind or ordered after frame success — the ESM-frozen-module-namespace limitation on spying `fs.renameSync` directly (see test file) made a call-order spy infeasible, so this asserts the observable outcome instead
- [x] 1.5 Write failing test: multi-scan batch with a mix of exported/skipped/failed scans in one call returns all three counts/collections correctly (including `failedScans` entries with identifying detail) and does not abort on the failed scan
- [x] 1.6 Write failing test: fatal path — an unwritable/inaccessible destination directory (batch-wide, not per-scan) returns `{ success: false, error }`
- [x] 1.7 Write failing test: the export query excludes soft-deleted scans (regression coverage — confirms the new dedicated query doesn't inherit the sibling `db:scans:list` legacy-branch bug this proposal deliberately avoids fixing directly)
- [x] 1.8 Write failing test: progress reporting — `webContents.send('db:scans:export-progress', { totalFiles, completedFiles, currentScanId })` fires once per file outcome (copied, skipped, or failed) with the correct running counts
- [x] 1.9 Implement `scansExport()` + `db:scans:export` in `database-handlers.ts`: dedicated `deleted: false` query; for each scan, resolve+containment-check source (`scans_dir` + `scan.path`, via `resolveScanPath` from `src/main/scan-protocol.ts`) and destination (`destinationDir` + `scan.path`, same function) paths — a scan failing either check is recorded in `failedScans`, not written; for scans that pass, create the destination directory, reorder the source's `readdir` listing so `metadata.json` comes first, and for each file: skip if it already exists at its final destination path, otherwise remove a stale `<filename>.tmp` if present, copy to `<filename>.tmp`, then `fs.renameSync` to `<filename>`; accumulate `exportedFiles`/`exportedScans`/`skippedFiles`/`failedScans`
- [x] 1.10 Wire progress reporting per 1.8's contract, following `graviscan:download-images`'s window-at-send-time pattern (`register-handlers.ts:468-479`) — required adding an optional `getMainWindow` parameter to `registerDatabaseHandlers()`, passed as `() => mainWindow` from `main.ts`, matching `initGraviScan`'s existing DI convention
- [x] 1.11 Run `npm run lint && npx tsc --noEmit && npm run test:unit` — confirm all tests from 1.1-1.8 pass and nothing else broke before moving to preload/renderer work. Lint and typecheck clean; full suite: 4 pre-existing failures (Windows path-separator assertion in `scan-coordinator.test.ts`, timing/typing issues in `AccessionForm.test.tsx`) confirmed present on a run excluding the new test file too — unrelated to this change. Had to isolate `scans-export.test.ts`'s real-SQLite `PrismaClient` onto a private copy of `dev.db` (vitest runs test files in parallel, and this file's destructive `cleanDatabase()` was racing `graviscan/database-handlers.test.ts`'s fixtures on the shared file, causing spurious FK-constraint failures) — documented in the test file itself.

## 2. Preload / types

- [x] 2.1 Add `scans.export(scanIds, destinationDir)` and `scans.onExportProgress(callback)` (returning a cleanup function) to `preload.ts`'s `databaseAPI.scans`, mirroring `downloadImages` / `onDownloadProgress`
- [x] 2.2 Extend `tests/unit/preload-listener-cleanup.test.ts` to support nested accessor paths before adding an entry for this handler: replaced the flat `{namespace, method}` shape with a `get: (exposedAPI) => fn` accessor per entry (all 9, including the new `database.scans.onExportProgress`), and changed both `it()` blocks to call `target.get(exposedAPI)(callback)` instead of the old two-level `exposedAPI[namespace][method]` lookup
- [x] 2.3 Update the `DatabaseAPI` type — added `ScansExportData`/`ScansExportFailure`/`ScansExportProgress` to `src/types/database.ts` and `scans.export`/`scans.onExportProgress` to the `DatabaseAPI` interface in `src/types/electron.d.ts` (the actual interface's location; `src/types/database.ts` holds the data-shape types it references)
- [x] 2.4 Run `npm run lint && npx tsc --noEmit && npm run test:unit` — 2.2 passes (18/18, all 9 entries × 2 tests); full suite still at the same 4 pre-existing failures as section 1's gate, nothing new broken

## 3. Renderer

- [x] 3.1 Write failing Vitest/@testing-library/react test for the group-header tri-state checkbox logic in isolation (checked when every scan in the group is selected, indeterminate when some but not all are, unchecked when none are) — this has no existing precedent in the codebase to lean on, so it needs direct unit coverage, not just an E2E pass
- [x] 3.2 Write failing Vitest/@testing-library/react test asserting each group header renders its scan-count label (e.g. "Experiment A — Jan 5, 2026 (15 scans)")
- [x] 3.3 Write failing Vitest/@testing-library/react test asserting that when `scans.export(...)` rejects or resolves with `{ success: false }`, the export button/spinner leaves its loading state and an error is shown — a direct regression test for the pilot's "stuck on Exporting... forever" bug
- [x] 3.4 Write failing Vitest/@testing-library/react test asserting that when a completed export's response includes a non-empty `failedScans`, the completion banner visibly lists the affected scans by experiment name and **full capture timestamp** (date and time — day-only would make two failed scans from the same group indistinguishable, per `design.md`) and distinguishes the failed count from the skipped count — not just the main-process-level count already covered by 1.5
- [x] 3.4b Write failing Vitest/@testing-library/react test asserting a persistent "don't disconnect the destination" warning is visible for the whole duration of an in-progress export and disappears once the completion (or fatal error) banner appears
- [x] 3.5 Write failing Vitest/@testing-library/react test that mocks `onExportProgress` and invokes its callback synchronously with a sequence of staged progress payloads, asserting the rendered progress indicator updates after each — this is the primary coverage for "progress updates as discrete steps," since a real E2E run may complete within a single event-loop turn on small fixtures and never observe an intermediate state
- [x] 3.6 Create `src/renderer/Export.tsx`: fetch all non-deleted scans across all scanners (no scanner-id filter), grouped by experiment × capture day with a per-group scan count (3.2); track a live selected-scan count; disable the export action until a destination and at least one scan are selected. Fetches via `db:scans:list`'s paginated branch looped over pages (100/page) rather than its legacy branch, since only the paginated branch filters `deleted: false` — this wasn't explicitly pinned down in design.md/tasks.md and was resolved during implementation.
- [x] 3.7 Implement the grouped list: per-scan checkbox plus the tri-state group-header checkbox from 3.1 (set via ref in a `useEffect`, per `design.md`)
- [x] 3.8 Wire the destination picker via `config:browse-directory`
- [x] 3.9 Wire the `scans.export(...)` call; subscribe to `onExportProgress` (with cleanup on unmount, verified by 3.11) to drive the progress indicator from 3.5 and the disconnect warning from 3.4b; show the completion banner (exported/skipped, plus the distinct failed-scan detail from 3.4) or the error banner from 3.3 on fatal failure
- [x] 3.10 Add the `export` route (matching #77) in `App.tsx` and the nav link in `Layout.tsx`
- [x] 3.11 Write failing Vitest/@testing-library/react test asserting the `onExportProgress` cleanup function is called when `Export.tsx` unmounts mid-export
- [x] 3.12 Run `npm run lint && npx tsc --noEmit && npm run test:unit` — confirm all tests from 3.1-3.5, 3.4b, and 3.11 pass and nothing else broke before moving to E2E. Lint/typecheck clean; full suite still at the same 4 pre-existing failures, 1342 passing (+10 from `Export.test.tsx`).

## 4. End-to-end tests

- [x] 4.1 Add `db:scans:export` coverage to `tests/e2e/renderer-database-ipc.e2e.ts` (required — the IPC coverage gate statically scans this file for the handler name or a `.scans.export(` call). Added a "Scans Export" describe block with a happy-path test and a path-containment-rejection test. Confirmed via `PYTHONIOENCODING=utf-8 python scripts/check-ipc-coverage.py`: `db:scans:export` now shows ✅ (2 test calls), overall coverage 95.7% (well above the 90% threshold; the 2 remaining untested handlers are pre-existing and unrelated).
- [x] 4.2 E2E test: with no scans selected and no destination chosen, the export action is disabled and the selected-count reads zero; selecting scans via per-scan and group-header checkboxes updates the count but the action stays disabled until a destination is also chosen
- [x] 4.3 E2E test: run an export to completion and assert the completion banner appears once done — confirmed the "Do not disconnect" warning's own visibility is inherently too transient to assert on reliably in E2E for a small/fast export (it disappeared before Playwright's next poll, exactly the flakiness risk this task called out), so that assertion was dropped from the E2E test in favor of its existing timing-controlled coverage in `Export.test.tsx`; this test is a pure smoke check that the full pipe works in a real Electron app
- [x] 4.4 E2E test: re-run an export against a destination that already has some _and_ all files from a prior run — verify the completion banner reports skips (not overwrites, not errors) in both cases, and file contents are unchanged
- [x] 4.5 E2E test: verify the empty-state message when no scans exist

## 5. Validation

- [x] 5.1 `openspec validate add-cylinderscan-export-page --strict` — passes
- [x] 5.2 Run the IPC coverage check and confirm it stays at or above the 90% threshold — 95.7% (44/46 handlers; the 2 untested are pre-existing, unrelated to this change)
- [x] 5.3 Full test suite green (`npm run lint && npx tsc --noEmit && npm run test:unit && npm run test:e2e`) — lint/typecheck clean; unit suite at the same 4 pre-existing failures throughout (1342 passing); full e2e suite: 263 passed, 4 skipped (pre-existing), 1 failed — `graviscan-ipc.e2e.ts`'s "Reset All USB Connections" test, confirmed unrelated (its own code comment documents real 5-10s hardware-safety delays as a known source of timing flakiness; nothing in this change touches GraviScan's USB-reset code path). All 6 new e2e tests (4 in `export-page.e2e.ts`, 2 in `renderer-database-ipc.e2e.ts`) passed.
- [ ] 5.4 After merge, comment on #77 noting the deliberate scanner-scoping deviation (see `proposal.md`'s "Deviation from #77") so the unchecked acceptance criterion reads as intentional, not forgotten
