## 1. Maker-artifact verification script

- [x] 1.1 Write the test first: `tests/unit/test-make-artifacts.test.ts`, mocking the filesystem (e.g. a temp dir standing in for `out/make/`), verifying the artifact-glob resolver (a) returns a match when a DMG/ZIP file exists under a macOS-shaped path, (b) returns a match when a `.exe`/`.nupkg` exists under a Windows-shaped path, (c) returns no match when the directory is empty, (d) returns no match when the only matching file is zero bytes (a maker that crashed mid-write must not pass), (e) picks the correct file when multiple artifacts exist (e.g. both a `.dmg` and a `.zip`). This test must fail before 1.2's implementation exists.
- [x] 1.2 Implement `tests/integration/test-make-artifacts.ts`: platform-aware glob under `out/make/` (macOS: `*.dmg`/`*.zip`; Windows: `*Setup*.exe`/`*.nupkg`), asserts the match is non-trivially sized, exits non-zero with a clear message if nothing matches or the match is empty. Make 1.1's test pass.
- [x] 1.3 Add `"test:make:artifacts": "ts-node tests/integration/test-make-artifacts.ts"` to `package.json`.

## 2. Packaged-app window-render + DB-init verification script

- [x] 2.1 Write the test first: `tests/unit/verify-db-tables.test.ts` for the `better-sqlite3`-based table-existence check (ported from `scripts/lib/verify-database.sh`'s `verify_table_exists`/`verify_schema`) — verify it returns "all present" for a temp SQLite DB created with the full expected schema, and returns the specific list of missing tables when one or more of several expected tables is absent (not just a single present/absent table). Must fail before 2.2's implementation exists.
- [x] 2.2 Implement the `better-sqlite3` table-verification helper (`tests/integration/lib/verify-db-tables.ts`) so 2.1 passes. Keep the same hardcoded/allowlisted expected-table list pattern `scripts/lib/verify-database.sh` uses (no dynamic/user-supplied table names).
- [x] 2.3 Write the test first: `tests/unit/wait-for-log-pattern.test.ts` for a small helper that resolves once a given pattern appears in a stream of text within a timeout, and rejects/times out if it doesn't — ported from `scripts/lib/test-utils.sh`'s `wait_for_log_pattern`, using a fake/mock readable stream fed chunks over fake timers. Must fail before 2.4's implementation exists.
- [x] 2.4 Implement `tests/integration/lib/wait-for-log-pattern.ts` so 2.3 passes.
- [x] 2.5 Write the test first: `tests/unit/resolve-staged-app-path.test.ts` for a pure function `(platform, arch) -> stagedAppBinaryPath`, covering the darwin/win32/linux cases already implicitly duplicated between `scripts/test-package-database-full.sh` and `tests/integration/test-package.ts` — extracting this into one tested helper instead of a third inline copy. Must fail before 2.6's implementation exists.
- [x] 2.6 Implement `tests/integration/lib/resolve-staged-app-path.ts` so 2.5 passes.
- [x] 2.7 Implement `tests/integration/test-packaged-app-launch.ts`, composed from the 2.2/2.6 helpers: at the top of the file, `delete process.env.ELECTRON_RUN_AS_NODE` before any Playwright call (required — see design.md Decision 2a). Resolve the staged app path (2.6), launch it via `@playwright/test`'s `_electron.launch({ executablePath })`, await `firstWindow()` with a timeout (the window-render assertion), then verify the database via the 2.2 helper. **Revised during 2.9's dry-run** (see below): the original plan to detect DB-init via the `wait-for-log-pattern` helper (2.3/2.4) against `electronApp.process().stdout` was dropped — empirically, that log line races with Playwright's own internal stdout consumption and is not reliably observable from this script (design.md Decision 2b). `wait-for-log-pattern.ts`/its test remain in the codebase (generically useful, still tested) but are not used by this script.
- [x] 2.8 Add `"test:package:launch": "ts-node tests/integration/test-packaged-app-launch.ts"` to `package.json`.
- [x] 2.9 Run both new scripts locally against a local `npm run make` output to confirm they pass against a real packaged app before wiring into CI. (Adapted: the implementation dev environment is Windows, not macOS — ran against a local Windows `npm run make` output instead; the macOS job still gets its first real verification from CI in task 3.5/6.3.) **This dry-run found three real issues, all resolved before this task was marked done — see tasks 2.10-2.13 and design.md Decisions 1b/2b/2c for full detail:**
  1. A genuine pre-existing Windows bug in `database.ts` (`EPERM` on `fs.symlinkSync`) that would have made `test-make-windows` fail on every future PR — fixed via a new `ensureSymlinkOrCopy()` helper (task 2.10).
  2. The DB-init log-line approach (task 2.7's original plan) doesn't reliably work from this script, for reasons unrelated to the symlink bug — replaced with polling the real database file (task 2.11).
  3. The script never ran `prisma migrate deploy` before launching (the packaged app never runs migrations itself), so there was no schema for it to find — fixed by adding that step (task 2.12).
- [x] 2.10 Fix the Windows symlink bug found by 2.9, with TDD: write `tests/unit/fs-symlink-or-copy.test.ts` first (mocking `fs.symlinkSync`/`fs.cpSync` — a real forced `EPERM` isn't portable, since the privilege state under test isn't guaranteed in any given environment): covers skip-if-exists, symlink-succeeds-no-copy, symlink-fails-falls-back-to-copy, and both-fail-throws. Confirmed failing (module missing), then implemented `src/main/fs-symlink-or-copy.ts`'s `ensureSymlinkOrCopy(target, linkPath, type)`. Replaced `database.ts`'s bare `fs.symlinkSync` + swallow-and-hope with a call to `ensureSymlinkOrCopy(...)`. Confirmed fix against the real packaged build: `resources/node_modules/@prisma/client` is now a real copied directory (not a symlink) containing `runtime/library.js`, and a standalone `require()` of the Prisma client from that location succeeds.
- [x] 2.11 Replace the log-line-based DB check with file-based polling, with TDD: write `tests/unit/wait-for-condition.test.ts` first for `waitForCondition(predicate, timeoutMs, intervalMs)` — resolves once the predicate returns true, treats a _throwing_ predicate as "not ready yet" and keeps retrying (mirrors polling for a file that doesn't exist yet — `better-sqlite3`'s `fileMustExist` throws rather than returning false), and rejects with a timeout error (including the last failure) if it never becomes true. Confirmed failing, then implemented `tests/integration/lib/wait-for-condition.ts`. Updated `test-packaged-app-launch.ts` to poll `fs.existsSync(dbPath) && verifyDbTables(dbPath).allPresent` instead of waiting on a log line.
- [x] 2.12 Add the missing external-migration step to `test-packaged-app-launch.ts`, mirroring `scripts/test-package-database-full.sh`'s Step 1: before launching, delete any stale `bloom.db`, ensure the parent directory exists, and run `npx prisma migrate deploy` with `BLOOM_DATABASE_URL=file:<dbPath>` via `execSync`. No new helper needed (thin, direct use of an existing CLI, same pattern the existing bash script already uses) — confirmed this was the actual missing piece by manually testing Prisma client module resolution and a raw `$connect()` against the packaged app's bundled Prisma client outside of Electron entirely, before concluding the fix belonged in the test script rather than the app.
- [x] 2.13 Fix `.gitignore`: a pre-existing blanket `lib/` rule (Python-packaging boilerplate, already carved out for `scripts/lib/`) was silently excluding the new `tests/integration/lib/` directory from git. Added `!tests/integration/lib/` alongside the existing `!scripts/lib/` exception. Confirmed via `git status` that the directory is now tracked.
- [x] 2.14 Re-ran `test:make:artifacts` and `test:package:launch` against a freshly rebuilt (`npx electron-forge make`) packaged Windows app after all of 2.10-2.12's fixes — both scripts pass end-to-end: maker artifact found (Squirrel `Setup.exe`, correctly preferred over the `.nupkg`), window renders, migrations apply, database verified intact with all expected tables.
- [x] 2.15 **Found during this change's own post-implementation `/review-pr` pass, then confirmed empirically (not just reasoned about) — see design.md Decision 2d:** the file-only DB check from 2.11/2.12 was structurally unable to catch the exact bug 2.10 fixes. Because `applySchemaExternally()` seeds a fully valid schema _before_ launch, and the app's own DB init never touches that file (2.9's finding), the post-launch file check would pass identically whether the app's database code path succeeded, failed, or never ran. Confirmed by temporarily reverting `database.ts` to the pre-2.10 buggy symlink-only code, rebuilding, and re-running `test-packaged-app-launch.ts`: window still rendered, file-only check still passed — a real false-negative, reproduced on demand. Fixed: added `verifyDatabaseIpcWorks()`, using Playwright's `page.evaluate()` to call the existing, read-only `window.electron.database.phenotypers.list()` IPC method from inside the running renderer after the window renders — this requires `registerDatabaseHandlers()` to have actually run and the Prisma connection to actually work. Re-ran the same regression (buggy `database.ts` + rebuild): the script now correctly fails with `"No handler registered for 'db:phenotypers:list'"`. Reverted `database.ts` back to the real fix (confirmed via `git diff` showing zero drift from the last commit) and reconfirmed the full script passes end-to-end one more time. The file-based check remains as secondary defense-in-depth, not the primary assertion.
- [x] 2.16 Fixed `resolveDbPath()`'s home-directory resolution (found during the same review pass): it used `process.env.HOME || process.env.USERPROFILE`, but the app resolves its own DB path via Electron's `app.getPath('home')` (OS-native, never consults `HOME` on Windows) — a shell environment (e.g. Git Bash, used throughout this implementation) can set `HOME` to a value that diverges from the real OS profile directory, silently pointing the check at a path the app never writes to. Switched to Node's `os.homedir()`, which is what `app.getPath('home')` is backed by.
- [x] 2.17 Fixed three smaller findings from the same review pass: (a) `tests/integration/lib/verify-db-tables.ts`'s `EXPECTED_TABLES` was missing `GraviExperimentWaveMetadata` (a real table added to `prisma/schema.prisma` after the bash-script list this was ported from was last updated) — added it; (b) `docs/PACKAGING.md` had a stale code listing and prose describing the pre-2.10 symlink-only behavior with no copy fallback — updated both the code sample and the "why" explanation; (c) `scripts/test-package-database-full.sh:232`'s log-grep for the old `"[Database] Created symlink"` message would have silently stopped matching once 2.10 changed the log prefix to `[fs-symlink-or-copy]` — updated the one grep line to match both the symlink-success and copy-fallback-success cases (cosmetic-only fix to a non-fatal diagnostic line; does not change what that job asserts or its pass/fail behavior, so it doesn't reopen the "leave this script untouched" non-goal).

## 3. CI workflow changes

- [x] 3.1 Add the `test-make` job to `.github/workflows/pr-checks.yml` (macOS): `needs: build-python`, `runs-on: macos-latest`, steps mirroring `test-package-database`'s setup (checkout, setup-node, cache, setup uv, download `python-executable-macOS` artifact, `npm ci`, `npx prisma generate`) but running `npm run make` instead of `npm run package`, followed by `npm run test:make:artifacts` and `npm run test:package:launch`.
- [x] 3.2 Add the `test-make-windows` job: same shape, `runs-on: windows-latest`, downloads `python-executable-Windows` (already produced by the existing `build-python` matrix — no changes needed there).
- [x] 3.3 Add both new jobs to `all-checks-passed`'s `needs` list.
- [x] 3.3a Add an `actions/upload-artifact` step to both new jobs, running _after_ `npm run test:make:artifacts` passes (so a failed/empty artifact is never uploaded), uploading `out/make/**` as `packaged-app-${{ runner.os }}` with a short retention (e.g. `retention-days: 7`, matching the existing Playwright-failure-artifact convention in this workflow — not the Python-executable artifact's 1-day retention, since these are meant for a human to actually download and manually test-install, not just cross-job reuse within the same run).
- [x] 3.4 Confirm `git diff` for `.github/workflows/pr-checks.yml` touches only additive lines around the new jobs and `all-checks-passed`'s `needs` list — the existing `test-package-database` job's own lines must show zero diff, matching the "existing job unchanged" non-goal in design.md. (Confirmed: `git diff --unified=0` shows exactly two pure-insertion hunks, at line 602 and line 617 — no existing line changed.)
- [x] 3.5 There is no automated way to unit-test a GitHub Actions workflow file's logic in isolation; the actual CI run on this change's PR (both new jobs going green against a real `npm run make`) is the test for this section. Confirmed: `Test - Make (Windows)` and `Test - Make (macOS)` both passed on PR #305's real CI run (first push: 10m50s/4m29s; second push, after the DB-verification fix: also passed), on the actual GitHub-hosted `windows-latest`/`macos-latest` runners, not just locally.
- [x] 3.6 Confirm empirically (from the real CI run in 3.5) that `windows-latest` renders a real window for a launched packaged Electron app with no extra runner configuration (per design.md's risk note). If it does not, stop and report back rather than weakening the window-render assertion to work around it. Confirmed: no extra runner configuration was needed — `Test - Make (Windows)` passed with the standard job setup.

## 4. Hardware documentation fixes

These are corrections that restore conformance with already-accepted OpenSpec requirements (`machine-configuration`'s "Basler acA2000-50gm Camera Compatibility" and "Scan Parameter Configuration", `scanning`'s metadata.json field requirement, `developer-workflows`' "Hardware Testing Command") — see design.md Decision 0. No spec delta accompanies these; they are bug fixes restoring intended, already-specified behavior in the docs.

- [x] 4.1 Fix `docs/DAQ_TESTING.md`: replace all `seconds_per_rot: 36.0` examples with the current default, `7.0` (matches `machine-configuration`'s "Scan Parameter Configuration" requirement; source of truth is `src/types/daq.ts`'s `DEFAULT_DAQ_SETTINGS.seconds_per_rot`).
- [x] 4.2 Update `docs/CAMERA_TESTING.md`'s description of `test-camera.ts` vs. what `npm run test:camera` actually runs today (see task 5) — this file has no stray `brightness`/`contrast` references to remove (confirmed absent via grep); do not add a "remove brightness" step here, there is nothing to remove.
- [x] 4.3 Fix `.claude/commands/hardware-testing.md`: remove all three stray `brightness` references (confirmed at lines 18, 138, 288 — a "brightness" field is not part of `CameraSettings` and is not documented by the already-accepted `machine-configuration` spec; line 138's "Adjust exposure, gain, brightness" step now reads "Adjust exposure, gain, gamma").
- [x] 4.4 Rewrite `docs/SCANNER_TESTING.md`'s camera-settings table/examples to match the real `CameraSettings` shape (`exposure_time`, `gain`, `camera_ip_address`, `gamma`, `num_frames`, `seconds_per_rot` — not the legacy `serial_number`/`width`/`height`/`pixel_format`/`trigger_mode` shape); add coverage of `ScannerSettings.metadata`/`ScanMetadata` and `metadata.json`'s atomic-write behavior; fix the inherited stale `seconds_per_rot` default. **When documenting `metadata.json`'s actual output fields, include `brightness: 0` and `contrast: 0` as fixed backward-compatibility identity values that are always written — do NOT state they are absent from `metadata.json`.** This is a real, current, already-specified behavior (`scanning` spec, `scan-metadata-json.ts:82-83`), distinct from `CameraSettings` (the user-configurable type) genuinely having no such fields. Getting this backwards would introduce a new factual error.
- [x] 4.5 Fix `docs/CONFIGURATION.md`: remove the nonexistent `brightness` field (and its bogus line reference) from the list of admin-configurable camera settings; correct `gain`'s documented default (100, not 0) and range (36-512 GainRaw integer, not "0-24 dB") per `machine-configuration`'s already-accepted spec and `python/hardware/camera_types.py`; add `experiment_type` (`prisma/schema.prisma`) to the documented configuration fields, including its default value. **Correction found during implementation:** the original task text said to "fix the Prisma/DB snippet" from `Float` to `Int` — that would have been WRONG. `prisma/schema.prisma`'s real `Scan.gain` column is genuinely `Float` (verified directly); the doc's existing snippet was already accurate to the schema. The real, separate issue is that the Python hardware layer enforces `gain` as an integer `GainRaw` at the application layer — a pre-existing DB-column-vs-application-layer type inconsistency, not a documentation error. Added a clarifying note instead of "fixing" a snippet that wasn't wrong.
- [x] 4.6 Add documentation for the scanner identity service (`src/main/main.ts`'s `scanner:get-scanner-id` handler) and idle-session-reset behavior (`src/main/idle-timer.ts`, `src/main/session-store.ts`) to the appropriate existing doc(s) — new documentation-completeness content, per the `hardware-testing-documentation` spec delta. (Added as new sections in `docs/SCANNER_TESTING.md`.)
- [x] 4.7 Create `docs/TROUBLESHOOTING.md` consolidating the troubleshooting sections currently scattered across `docs/CAMERA_TESTING.md`, `docs/DAQ_TESTING.md`, `docs/SCANNER_TESTING.md`, and `docs/PACKAGED_APP_TESTING.md`. (Each source doc's Troubleshooting section trimmed to a link into the new consolidated doc, rather than left duplicated.)
- [x] 4.8 Verification for this section (docs have no automated test suite of their own, but these are scriptable, concrete checks — note: "brightness" as an English word describing image appearance is legitimate prose in several places, e.g. "affects image brightness"; the check below targets the specific stale CLAIM that `brightness` is a configurable `CameraSettings` field, not the bare word): run `grep -c "brightness" docs/CAMERA_TESTING.md .claude/commands/hardware-testing.md` and confirm each returns `0` (verified: both return 0); run `grep -n "\`brightness\`" docs/CONFIGURATION.md`and confirm no table row or line-reference claiming`brightness`is a`CameraSettings`field remains — only the intentional "not configurable" clarifying note should reference it (verified: only the Decision-3-driven clarifying note remains, no field-row claim); run`grep -c "brightness" docs/SCANNER_TESTING.md`and confirm it returns`1`or more (the intentional "metadata.json always writes brightness: 0" coverage added in 4.4); run`grep -rn "36.0" docs/DAQ_TESTING.md`and confirm zero hits (verified); run`grep -n "experiment_type" docs/CONFIGURATION.md` and confirm at least one hit (verified).

## 5. Re-wire orphaned camera integration test

- [x] 5.1 Add `"test:camera:connect": "ts-node tests/integration/test-camera.ts"` to `package.json`. (Matched the existing `test:daq`/`test:streaming` convention of prefixing with `bash scripts/ensure-python-executable.sh &&`.)
- [x] 5.2 Update `docs/CAMERA_TESTING.md` to accurately state that `npm run test:camera` runs the streaming test (`test-streaming.ts`) and `npm run test:camera:connect` runs the connect/capture/configure/disconnect test (`test-camera.ts`) — matches the `developer-workflows` MODIFIED delta's new "Developer distinguishes camera test scripts" scenario.
- [x] 5.3 Update `.claude/commands/integration-testing.md` — the actual file backing the `/integration-testing` command that the MODIFIED "Integration Testing Command" requirement governs. Added a `test:camera:connect` entry (alongside the existing `### Camera Integration Test` section's `npm run test:camera` / `Alias: npm run test:streaming` block) distinguishing streaming coverage from connect/capture/configure/disconnect coverage, and added `npm run test:camera:connect` to the "Running All Integration Tests" list.

## 6. Validation

- [x] 6.1 Run `npm run lint`, `npx tsc --noEmit`, and `npm run test:unit` (existing unit suite, including the new tests from 1.1/2.1/2.3/2.5/2.10/2.11) — all green. `npm run lint` and `npx tsc --noEmit` produced zero output (clean). `npm run test:unit`: 1264 passed, 7 failed, 91 skipped — confirmed via `git stash` that the exact same 5 files / 7 tests fail identically on a clean checkout of `main` with none of this change's edits applied, so these are pre-existing, unrelated failures, not regressions from this change. All 6 new test files (27 tests) pass.
- [x] 6.2 Run `openspec validate add-cylinderscan-packaging-ci --strict` and fix any issues. (Valid.)
- [x] 6.3 Push the branch and confirm the new `test-make` and `test-make-windows` CI jobs pass on the real PR (this is the actual test for the CI changes, per task 3.5), and confirm the existing `test-package-database` job's own run is unaffected (per task 3.4). Confirmed on both pushes to PR #305: `Test - Make (Windows)` and `Test - Make (macOS)` passed; `Test - Packaged App Database (macOS)` (the existing, untouched job) also passed, unaffected. PR #305 merged to `main` at `2a9c0e8`.
