## 1. `ScannerSubprocess.shutdown()` becomes honest about confirmation (design.md Decision 3)

- [ ] 1.1 In `tests/unit/graviscan/scanner-subprocess.test.ts`, in the existing `describe('shutdown()', ...)` block: add a failing test — construct a subprocess, spawn it (mock the child process so it never emits `exit` after `quit()`/`kill()` are called, i.e. simulate a truly stuck process), call `shutdown(shortTimeoutMs)`, and assert the returned promise resolves to `false` (not `true`, and not `undefined`/void) after both the graceful-quit timeout and the post-kill confirmation window elapse. Confirm it fails against current code (today's `shutdown()` resolves with no boolean at all).
- [ ] 1.2 Add a failing test: same setup, but the mocked child process DOES emit `exit` shortly after `kill()` is called (within the confirmation window). Assert `shutdown()` resolves to `true`.
- [ ] 1.3 Add a failing test: the process exits gracefully within the original `timeoutMs` (in response to `quit()`, no force-kill needed). Assert `shutdown()` resolves to `true` and `kill()` is never called.
- [ ] 1.4 Add a failing test extending the existing "resolves immediately if already dead" case: assert the resolved value is `true` (not just that it resolves), for both the `dead` and `idle` (never-spawned) states.
- [ ] 1.5 Implement: change `shutdown()`'s signature to `Promise<boolean>` in `src/main/graviscan/scanner-subprocess.ts` (design.md Decision 3's two-phase timeout: graceful-quit timeout → force-kill → bounded `KILL_CONFIRM_TIMEOUT_MS` confirmation wait on the real `exit` event). Add the `KILL_CONFIRM_TIMEOUT_MS = 2_000` module-level constant. Do not change `kill()`'s own behavior (it still sets `state = 'dead'` synchronously — `killAll()`'s app-quit fast path depends on that). Run 1.1-1.4 and confirm green.
- [ ] 1.6 Run the full existing `scanner-subprocess.test.ts` suite and confirm no regressions (existing shutdown tests that only checked "resolves" without inspecting the value must still pass under the new `Promise<boolean>` signature).

## 2. Per-scannerId in-flight-spawn guard (design.md Decision 1)

- [ ] 2.1 In `tests/unit/graviscan/scan-coordinator.test.ts`, add a new test double: a controllable-delay mock `ScannerSubprocess` whose `spawn()` returns a promise that stays pending until the test explicitly resolves or rejects it (extending the existing `vi.mock`'d `ScannerSubprocess` / `createMockSubprocess()` helper — check how the mock module factory is structured before adding a second flavor, per the existing helper's own pattern). No test today simulates a still-spawning (not-yet-settled) subprocess; add this fixture as its own helper (e.g. `createPendingMockSubprocess()`) used by the tests below.
- [ ] 2.2 Add a failing test: call `coordinator.initialize([{scannerId: 'A', ...}])` twice concurrently (without awaiting the first) using the pending mock from 2.1 for `'A'`. Before resolving the mock's `spawn()`, assert only one `ScannerSubprocess` constructor call happened for `'A'` and `existing.shutdown()` was never called on it. Then resolve the mock to `ready` and assert both `initialize()` calls' returned promises resolve, and exactly one subprocess is in the map for `'A'`.
- [ ] 2.3 Add a failing test: the same scenario as 2.2, but the second concurrent call is `addScanner({scannerId: 'A', ...})` instead of a second `initialize()` (matches the new "Concurrent addScanner and initialize calls for the same id" spec scenario). Assert the same outcome: exactly one subprocess, no `shutdown()` call on the in-flight one, both promises resolve together.
- [ ] 2.4 Add a failing test: two concurrent `addScanner()` calls for the same new `scannerId` while `isScanning === false` (today's `pendingAdds` queue only dedupes while `isScanning === true` — this is the previously-unguarded idle-state path). Assert exactly one subprocess is constructed.
- [ ] 2.5 Implement: add `private spawnInFlight = new Map<string, Promise<void>>()` to `ScanCoordinator`; split `spawnSingleScanner()` into the thin guarded wrapper plus `doSpawnSingleScanner()` per design.md Decision 1. Run 2.2-2.4 and confirm green.
- [ ] 2.6 Run the full existing `scan-coordinator.test.ts` suite and confirm no regressions, in particular the existing "Two concurrent addScanner calls for the same id" (mid-scan queueing) test — the new choke-point guard must not interfere with the separate `pendingAdds` mid-scan queueing mechanism, which stays in place unchanged (design.md notes these are independent, additive guards).

## 3. Bounded spawn-ready timeout with no-duplicate reclaim (design.md Decisions 2 & 3)

- [ ] 3.1 Add a failing test to `scan-coordinator.test.ts`: using the pending-mock fixture from 2.1, construct a mock whose `spawn()` never settles (neither resolves nor rejects) for longer than the coordinator's spawn-ready timeout. Use `vi.useFakeTimers()` / `vi.advanceTimersByTimeAsync()` (check existing timer-mocking conventions already used elsewhere in this test file, e.g. the `SCAN_ROW_TIMEOUT_MS` tests, before introducing a new pattern) to advance past `SPAWN_READY_TIMEOUT_MS`. Assert the mock's `shutdown()` is called (attempted reclaim), `initErrors.get(scannerId)` is set, a `scanner-init-status` event with `status: 'error'` is emitted for that scanner, and — critically — no second `ScannerSubprocess` constructor call happens for that `scannerId`.
- [ ] 3.2 Add a failing test: same as 3.1, but the mock's `shutdown()` (once called during reclaim) resolves to `false` (unconfirmed exit — see task 1). Assert the outcome is identical to 3.1 (error recorded, no duplicate spawn) regardless of the confirmation result, per design.md Decision 3 ("does not attempt to spawn a replacement... regardless of whether shutdown() returned true or false").
- [ ] 3.3 Add a failing test asserting no unhandled-rejection warning/crash occurs: after the spawn-ready timeout fires and the coordinator moves on, have the abandoned mock `spawn()` promise reject asynchronously (simulating the real subprocess's `exit`/`process-error` listeners firing after `reclaimUnresponsive()`'s `kill()`). Use a process-level `unhandledRejection` listener in the test (added and removed via `beforeEach`/`afterEach`) to assert none fires. This directly tests the pitfall called out in design.md Decision 2.
- [ ] 3.4 Add a failing test: a spawn that settles (`ready` or a normal rejection) comfortably before the timeout is unaffected — assert existing "spawn succeeds"/"spawn fails with ENOENT"-style tests (already in the suite) still pass unchanged, i.e. the timeout wrapper is not observable in the fast path.
- [ ] 3.5 Implement: add the `SPAWN_READY_TIMEOUT_MS = 45_000` module-level constant and a `withTimeout()` helper (or equivalent) in `scan-coordinator.ts` per design.md Decision 2, including the no-op `.catch()` on the raced-away `spawn()` promise. Implement `reclaimUnresponsive(scannerId, sub)` per design.md Decision 3 (calls `shutdown()`, deletes from `this.subprocesses` regardless of the result, records `initErrors`, emits `scanner-init-status` error — reusing the exact same reporting calls the existing catch-block already makes for other spawn failures, not new copy). Wire both into `doSpawnSingleScanner()`. Run 3.1-3.4 and confirm green.

## 4. Consistency fix: `initialize()`'s stale-subprocess cleanup logs unconfirmed shutdowns (design.md Decision 3)

- [ ] 4.1 Add a failing test to `scan-coordinator.test.ts`'s `initialize()` describe block: a scanner present in the coordinator's current subprocess map but absent from a new `initialize(scanners)` call's config list, whose mock `shutdown()` resolves to `false`. Assert a warning is logged identifying that scanner (spy on `console.warn` or the project's existing logging call, matching whatever the "Shutting down stale subprocess" log already uses) and that the entry is still removed from the subprocess map (control flow unchanged — only the honesty of the log changes, per design.md).
- [ ] 4.2 Implement: update the stale-subprocess cleanup loop (`scan-coordinator.ts` initialize(), current line ~152-159) to inspect `shutdown()`'s now-boolean return value and log a warning when `false`. Run 4.1 and confirm green.

## 5. Parallelize `initialize()`'s per-scanner spawn loop (design.md Decision 4, closes #144)

- [ ] 5.1 Add a failing test to `scan-coordinator.test.ts`'s `initialize()` describe block: initialize with 3 scanners whose mocked `spawn()` calls each take a distinct, controllable delay (e.g. 10ms/20ms/30ms via fake timers). Assert total elapsed time for `initialize()` to resolve is bounded by the slowest scanner's delay (e.g. ~30ms), not the sum (~60ms) — this is the direct regression test for #144.
- [ ] 5.2 Add a failing test: 3 scanners where the middle one's `spawn()` rejects (ENOENT-style) while the other two resolve to `ready`. Assert all three spawn attempts still run (none is skipped because an earlier one in iteration order failed), the failing one is recorded in `initErrors`, and the other two end up `ready` — i.e. partial failure under concurrent spawning does not block or skip the others (matches #144's explicit "if one scanner fails, the others should still initialize successfully").
- [ ] 5.3 Implement: replace the sequential `for` loop in `initialize()` with `Promise.allSettled(scanners.map((s) => this.spawnSingleScanner(s)))` per design.md Decision 4, including the defensive log for any unexpectedly-`'rejected'` settled result. Remove the now-inapplicable `if (this.cancelled) break;` per-iteration check (design.md notes this is a no-op removal, not a behavior change — confirm no existing test exercises `cancelAll()` during `initialize()` itself before removing it). Run 5.1-5.2 and confirm green.
- [ ] 5.4 Run the full existing `scan-coordinator.test.ts` suite and confirm no regressions, in particular any existing `initialize()` tests that asserted or relied on spawn *order* (sequential completion order is no longer guaranteed under concurrent spawning) — adjust any such assertions to check the resulting state (which scanners ended up `ready`/errored) rather than call order, without weakening what they verify.

## 6. Spec and documentation sync

- [ ] 6.1 Apply the spec deltas in `openspec/changes/fix-graviscan-scanner-init-race/specs/scanning/spec.md` to `openspec/specs/scanning/spec.md` (via `openspec archive` at the end of this change, not manually before then).
- [ ] 6.2 Run `npx openspec validate fix-graviscan-scanner-init-race --strict` and resolve any issues.

## 7. Verification sweep

- [ ] 7.1 Run `npm run format:check` (or `./node_modules/.bin/prettier --check`) — never bare `npx prettier`.
- [ ] 7.2 Run lint (`npm run lint` / the `/lint` command) and fix any issues.
- [ ] 7.3 Run `npx tsc --noEmit` and confirm no type errors from the `shutdown()` signature change or the new coordinator fields/helpers.
- [ ] 7.4 Run the full unit test suite for `tests/unit/graviscan/` and confirm all green, including every test added in sections 1-5.
- [ ] 7.5 Run any targeted GraviScan E2E spec(s) covering scanner init/Capture Scan locally for a real-Electron sanity check (per project convention, the full `npm run test:e2e` suite is CI-only — do not attempt to run it locally).
