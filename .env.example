# Bloom Desktop Database Configuration
#
# Copy this file to .env and configure for your local development environment
# The .env file is loaded by the Electron main process in development mode
#
# Database paths:
# - Development: Configured via BLOOM_DATABASE_URL below
# - Production: ~/.bloom/data/bloom.db (hardcoded in src/main/database.ts)
# - E2E Tests: Configured via .env.e2e
# - Integration Tests: Custom paths passed directly to initializeDatabase()
#
# For Prisma CLI commands (studio, migrate, etc.), use relative path:
# - npx prisma studio (database browser)
# - npx prisma migrate dev (create migrations)
# - npx prisma db push (development schema sync)
#
# URL Format (IMPORTANT):
# - Use file: protocol with proper URL format
# - Relative paths: file:./path/to/db or file:../path/to/db
# - Absolute paths: file:/absolute/path/to/db (Unix) or file:///C:/path/to/db (Windows)
# - The database.ts module uses URL() parsing to correctly handle all formats
# - NEVER use regex to parse file:// URLs (breaks absolute paths)

BLOOM_DATABASE_URL="file:./prisma/dev.db"
