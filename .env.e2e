# E2E Test Environment Variables
# Used by Playwright tests for configuration

# Database configuration for E2E tests
# Path is resolved from project root by both Prisma and Electron
BLOOM_DATABASE_URL="file:tests/e2e/test.db"

# Set to test mode for E2E tests (prevents DevTools from opening)
NODE_ENV=test
